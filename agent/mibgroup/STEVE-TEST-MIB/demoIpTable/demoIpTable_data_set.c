/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "demoIpTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table demoIpTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * STEVE-TEST-MIB::demoIpTable is subid 1 of products.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.12345.1.1, length: 9
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (demoIpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
demoIpTable_undo_setup( demoIpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:demoIpTable:demoIpTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup demoIpTable undo.
     * set up demoIpTable undo information, in preparation for a set.
     * Undo storage is in (* demoMacAddress_val_ptr_ptr )*
     */

    return rc;
} /* demoIpTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (demoIpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
demoIpTable_undo( demoIpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:demoIpTable:demoIpTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> demoIpTable undo.
     * demoIpTable undo information, in response to a failed set.
     * Undo storage is in (* demoMacAddress_val_ptr_ptr )*
     */

    return rc;
} /* demoIpTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (demoIpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
demoIpTable_undo_cleanup( demoIpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:demoIpTable:demoIpTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup demoIpTable undo.
     * Undo storage is in (* demoMacAddress_val_ptr_ptr )*
     */

    return rc;
} /* demoIpTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * demoIpTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param demoIpTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
demoIpTable_commit( demoIpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:demoIpTable:demoIpTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit demoIpTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_DEMOIPINUSE_FLAG) {
       save_flags &= ~COLUMN_DEMOIPINUSE_FLAG; /* clear demoIpInuse */
       /*
        * TODO:482:o: |-> commit column demoIpInuse.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"demoIpTable column demoIpInuse commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo demoIpInuse
             */
            rowreq_ctx->column_set_flags |= COLUMN_DEMOIPINUSE_FLAG;
       }
    }

    if (save_flags & COLUMN_DEMOIPADDRESS_FLAG) {
       save_flags &= ~COLUMN_DEMOIPADDRESS_FLAG; /* clear demoIpAddress */
       /*
        * TODO:482:o: |-> commit column demoIpAddress.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"demoIpTable column demoIpAddress commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo demoIpAddress
             */
            rowreq_ctx->column_set_flags |= COLUMN_DEMOIPADDRESS_FLAG;
       }
    }

    if (save_flags & COLUMN_DEMOMACADDRESS_FLAG) {
       save_flags &= ~COLUMN_DEMOMACADDRESS_FLAG; /* clear demoMacAddress */
       /*
        * TODO:482:o: |-> commit column demoMacAddress.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"demoIpTable column demoMacAddress commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo demoMacAddress
             */
            rowreq_ctx->column_set_flags |= COLUMN_DEMOMACADDRESS_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* demoIpTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * demoIpTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param demoIpTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
demoIpTable_undo_commit( demoIpTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:demoIpTable:demoIpTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo demoIpTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* demoIpTable_undo_commit */

/*
 * TODO:440:M: Implement demoIpTable node value checks.
 * TODO:450:M: Implement demoIpTable undo functions.
 * TODO:460:M: Implement demoIpTable set functions.
 * TODO:480:M: Implement demoIpTable commit functions.
 */
/*---------------------------------------------------------------------
 * STEVE-TEST-MIB::demoIpEntry.demoIpInuse
 * demoIpInuse is subid 2 of demoIpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.12345.1.1.1.2
 * Description:
Demo IP be in used
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER32 (based on perltype INTEGER32)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param demoIpInuse_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * demoIpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
demoIpInuse_check_value( demoIpTable_rowreq_ctx *rowreq_ctx, long demoIpInuse_val)
{
    DEBUGMSGTL(("verbose:demoIpTable:demoIpInuse_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid demoIpInuse value.
     */

    return MFD_SUCCESS; /* demoIpInuse value not illegal */
} /* demoIpInuse_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (demoIpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * demoIpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
demoIpInuse_undo_setup( demoIpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:demoIpTable:demoIpInuse_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup demoIpInuse undo.
     */
    /*
     * copy demoIpInuse data
     * set rowreq_ctx->undo->demoIpInuse from rowreq_ctx->data.demoIpInuse
     */
    rowreq_ctx->undo->demoIpInuse = rowreq_ctx->data.demoIpInuse;


    return MFD_SUCCESS;
} /* demoIpInuse_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param demoIpInuse_val
 *        A long containing the new value.
 */
int
demoIpInuse_set( demoIpTable_rowreq_ctx *rowreq_ctx, long demoIpInuse_val )
{

    DEBUGMSGTL(("verbose:demoIpTable:demoIpInuse_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set demoIpInuse value.
     * set demoIpInuse value in rowreq_ctx->data
     */
    rowreq_ctx->data.demoIpInuse = demoIpInuse_val;

    return MFD_SUCCESS;
} /* demoIpInuse_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
demoIpInuse_undo( demoIpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:demoIpTable:demoIpInuse_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up demoIpInuse undo.
     */
    /*
     * copy demoIpInuse data
     * set rowreq_ctx->data.demoIpInuse from rowreq_ctx->undo->demoIpInuse
     */
    rowreq_ctx->data.demoIpInuse = rowreq_ctx->undo->demoIpInuse;

    
    return MFD_SUCCESS;
} /* demoIpInuse_undo */

/*---------------------------------------------------------------------
 * STEVE-TEST-MIB::demoIpEntry.demoIpAddress
 * demoIpAddress is subid 3 of demoIpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.12345.1.1.1.3
 * Description:
Demo IP Address
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param demoIpAddress_val_ptr
 *        A char containing the new value.
 * @param demoIpAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by demoIpAddress_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * demoIpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.demoIpAddress).
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
demoIpAddress_check_value( demoIpTable_rowreq_ctx *rowreq_ctx, char *demoIpAddress_val_ptr,  size_t demoIpAddress_val_ptr_len)
{
    DEBUGMSGTL(("verbose:demoIpTable:demoIpAddress_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != demoIpAddress_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid demoIpAddress value.
     */

    return MFD_SUCCESS; /* demoIpAddress value not illegal */
} /* demoIpAddress_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (demoIpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * demoIpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
demoIpAddress_undo_setup( demoIpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:demoIpTable:demoIpAddress_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup demoIpAddress undo.
     */
    /*
     * copy demoIpAddress and demoIpAddress_len data
     * set rowreq_ctx->undo->demoIpAddress from rowreq_ctx->data.demoIpAddress
     */
    memcpy( rowreq_ctx->undo->demoIpAddress, rowreq_ctx->data.demoIpAddress,
            (rowreq_ctx->data.demoIpAddress_len * sizeof(rowreq_ctx->undo->demoIpAddress[0])));
    rowreq_ctx->undo->demoIpAddress_len = rowreq_ctx->data.demoIpAddress_len;


    return MFD_SUCCESS;
} /* demoIpAddress_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param demoIpAddress_val_ptr
 *        A char containing the new value.
 * @param demoIpAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by demoIpAddress_val_ptr
 */
int
demoIpAddress_set( demoIpTable_rowreq_ctx *rowreq_ctx, char *demoIpAddress_val_ptr,  size_t demoIpAddress_val_ptr_len )
{

    DEBUGMSGTL(("verbose:demoIpTable:demoIpAddress_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != demoIpAddress_val_ptr);

    /*
     * TODO:461:M: |-> Set demoIpAddress value.
     * set demoIpAddress value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.demoIpAddress, demoIpAddress_val_ptr, demoIpAddress_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.demoIpAddress_len = demoIpAddress_val_ptr_len / sizeof(demoIpAddress_val_ptr[0]);

    return MFD_SUCCESS;
} /* demoIpAddress_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
demoIpAddress_undo( demoIpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:demoIpTable:demoIpAddress_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up demoIpAddress undo.
     */
    /*
     * copy demoIpAddress and demoIpAddress_len data
     * set rowreq_ctx->data.demoIpAddress from rowreq_ctx->undo->demoIpAddress
     */
    memcpy( rowreq_ctx->data.demoIpAddress, rowreq_ctx->undo->demoIpAddress,
            (rowreq_ctx->undo->demoIpAddress_len * sizeof(rowreq_ctx->data.demoIpAddress[0])));
    rowreq_ctx->data.demoIpAddress_len = rowreq_ctx->undo->demoIpAddress_len;

    
    return MFD_SUCCESS;
} /* demoIpAddress_undo */

/*---------------------------------------------------------------------
 * STEVE-TEST-MIB::demoIpEntry.demoMacAddress
 * demoMacAddress is subid 4 of demoIpEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.12345.1.1.1.4
 * Description:
Demo MAC Address
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param demoMacAddress_val_ptr
 *        A char containing the new value.
 * @param demoMacAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by demoMacAddress_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * demoIpTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.demoMacAddress).
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
demoMacAddress_check_value( demoIpTable_rowreq_ctx *rowreq_ctx, char *demoMacAddress_val_ptr,  size_t demoMacAddress_val_ptr_len)
{
    DEBUGMSGTL(("verbose:demoIpTable:demoMacAddress_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != demoMacAddress_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid demoMacAddress value.
     */

    return MFD_SUCCESS; /* demoMacAddress value not illegal */
} /* demoMacAddress_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (demoIpTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * demoIpTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
demoMacAddress_undo_setup( demoIpTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:demoIpTable:demoMacAddress_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup demoMacAddress undo.
     */
    /*
     * copy demoMacAddress and demoMacAddress_len data
     * set rowreq_ctx->undo->demoMacAddress from rowreq_ctx->data.demoMacAddress
     */
    memcpy( rowreq_ctx->undo->demoMacAddress, rowreq_ctx->data.demoMacAddress,
            (rowreq_ctx->data.demoMacAddress_len * sizeof(rowreq_ctx->undo->demoMacAddress[0])));
    rowreq_ctx->undo->demoMacAddress_len = rowreq_ctx->data.demoMacAddress_len;


    return MFD_SUCCESS;
} /* demoMacAddress_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param demoMacAddress_val_ptr
 *        A char containing the new value.
 * @param demoMacAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by demoMacAddress_val_ptr
 */
int
demoMacAddress_set( demoIpTable_rowreq_ctx *rowreq_ctx, char *demoMacAddress_val_ptr,  size_t demoMacAddress_val_ptr_len )
{

    DEBUGMSGTL(("verbose:demoIpTable:demoMacAddress_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != demoMacAddress_val_ptr);

    /*
     * TODO:461:M: |-> Set demoMacAddress value.
     * set demoMacAddress value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.demoMacAddress, demoMacAddress_val_ptr, demoMacAddress_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.demoMacAddress_len = demoMacAddress_val_ptr_len / sizeof(demoMacAddress_val_ptr[0]);

    return MFD_SUCCESS;
} /* demoMacAddress_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
demoMacAddress_undo( demoIpTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:demoIpTable:demoMacAddress_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up demoMacAddress undo.
     */
    /*
     * copy demoMacAddress and demoMacAddress_len data
     * set rowreq_ctx->data.demoMacAddress from rowreq_ctx->undo->demoMacAddress
     */
    memcpy( rowreq_ctx->data.demoMacAddress, rowreq_ctx->undo->demoMacAddress,
            (rowreq_ctx->undo->demoMacAddress_len * sizeof(rowreq_ctx->data.demoMacAddress[0])));
    rowreq_ctx->data.demoMacAddress_len = rowreq_ctx->undo->demoMacAddress_len;

    
    return MFD_SUCCESS;
} /* demoMacAddress_undo */

/** @} */
