/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $ 
 *
 * $Id:$
 */
/*
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 * ***                                                               ***
 * ***  NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE NOTE  ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THIS FILE DOES NOT CONTAIN ANY USER EDITABLE CODE.      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***       THE GENERATED CODE IS INTERNAL IMPLEMENTATION, AND      ***
 * ***                                                               ***
 * ***                                                               ***
 * ***    IS SUBJECT TO CHANGE WITHOUT WARNING IN FUTURE RELEASES.   ***
 * ***                                                               ***
 * ***                                                               ***
 * *********************************************************************
 * *********************************************************************
 * *********************************************************************
 */

/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "clabWIFISSIDTable.h"


#include <net-snmp/agent/table_container.h>
#include <net-snmp/library/container.h>

#include "clabWIFISSIDTable_interface.h"
#if 0
netsnmp_feature_require(baby_steps)
netsnmp_feature_require(row_merge)
netsnmp_feature_require(check_all_requests_error)

#include <ctype.h>

/**********************************************************************
 **********************************************************************
 ***
 *** Table clabWIFISSIDTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * CLAB-WIFI-MIB::clabWIFISSIDTable is subid 4 of clabWIFIObjects.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4, length: 12
*/
typedef struct clabWIFISSIDTable_interface_ctx_s {

   netsnmp_container              *container;

   clabWIFISSIDTable_registration *      user_ctx;
   
   netsnmp_table_registration_info  tbl_info;

   netsnmp_baby_steps_access_methods access_multiplexer;

    u_int                             table_dirty;

} clabWIFISSIDTable_interface_ctx;

static clabWIFISSIDTable_interface_ctx clabWIFISSIDTable_if_ctx;

static void _clabWIFISSIDTable_container_init(
    clabWIFISSIDTable_interface_ctx *if_ctx);
static void _clabWIFISSIDTable_container_shutdown(
    clabWIFISSIDTable_interface_ctx *if_ctx);


netsnmp_container *
clabWIFISSIDTable_container_get( void )
{
    return clabWIFISSIDTable_if_ctx.container;
}

clabWIFISSIDTable_registration *
clabWIFISSIDTable_registration_get( void )
{
    return clabWIFISSIDTable_if_ctx.user_ctx;
}

clabWIFISSIDTable_registration *
clabWIFISSIDTable_registration_set( clabWIFISSIDTable_registration * newreg )
{
    clabWIFISSIDTable_registration * old = clabWIFISSIDTable_if_ctx.user_ctx;
    clabWIFISSIDTable_if_ctx.user_ctx = newreg;
    return old;
}

int
clabWIFISSIDTable_container_size( void )
{
    return CONTAINER_SIZE(clabWIFISSIDTable_if_ctx.container);
}

u_int
clabWIFISSIDTable_dirty_get( void )
{
    return clabWIFISSIDTable_if_ctx.table_dirty;
}

void
clabWIFISSIDTable_dirty_set( u_int status )
{
    DEBUGMSGTL(("clabWIFISSIDTable:clabWIFISSIDTable_dirty_set",
                "called. was %d, now %d\n",
                clabWIFISSIDTable_if_ctx.table_dirty, status));
    clabWIFISSIDTable_if_ctx.table_dirty = status;
}

/*
 * mfd multiplexer modes
 */
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_pre_request;
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_post_request;
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_object_lookup;
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_get_values;
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_check_objects;
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_undo_setup;
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_set_values;
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_undo_cleanup;
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_undo_values;
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_commit;
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_undo_commit;
static Netsnmp_Node_Handler _mfd_clabWIFISSIDTable_irreversible_commit;

clabWIFISSIDTable_data *clabWIFISSIDTable_allocate_data(void);

/**
 * @internal
 * Initialize the table clabWIFISSIDTable 
 *    (Define its contents and how it's structured)
 */
void
_clabWIFISSIDTable_initialize_interface(clabWIFISSIDTable_registration * reg_ptr,  u_long flags)
{
    netsnmp_baby_steps_access_methods *access_multiplexer =
        &clabWIFISSIDTable_if_ctx.access_multiplexer;
    netsnmp_table_registration_info *tbl_info = &clabWIFISSIDTable_if_ctx.tbl_info;
    netsnmp_handler_registration *reginfo;
    netsnmp_mib_handler *handler;
    int    mfd_modes = 0;

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_clabWIFISSIDTable_initialize_interface","called\n"));


    /*************************************************
     *
     * save interface context for clabWIFISSIDTable
     */
    /*
     * Setting up the table's definition
     */
    netsnmp_table_helper_add_indexes(tbl_info,
                                  ASN_INTEGER, /** index: clabWIFISSIDId */
                             0);

    /*  Define the minimum and maximum accessible columns.  This
        optimizes retrieval. */
    tbl_info->min_column = CLABWIFISSIDTABLE_MIN_COL;
    tbl_info->max_column = CLABWIFISSIDTABLE_MAX_COL;

    /*
     * save users context
     */
    clabWIFISSIDTable_if_ctx.user_ctx = reg_ptr;

    /*
     * call data access initialization code
     */
    clabWIFISSIDTable_init_data(reg_ptr);

    /*
     * set up the container
     */
    _clabWIFISSIDTable_container_init(&clabWIFISSIDTable_if_ctx);
    if (NULL == clabWIFISSIDTable_if_ctx.container) {
        snmp_log(LOG_ERR,"could not initialize container for clabWIFISSIDTable\n");
        return;
    }
    
    /*
     * access_multiplexer: REQUIRED wrapper for get request handling
     */
    access_multiplexer->object_lookup = _mfd_clabWIFISSIDTable_object_lookup;
    access_multiplexer->get_values = _mfd_clabWIFISSIDTable_get_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->pre_request = _mfd_clabWIFISSIDTable_pre_request;
    access_multiplexer->post_request = _mfd_clabWIFISSIDTable_post_request;

#ifndef NETSNMP_DISABLE_SET_SUPPORT

    /*
     * REQUIRED wrappers for set request handling
     */
    access_multiplexer->object_syntax_checks = _mfd_clabWIFISSIDTable_check_objects;
    access_multiplexer->undo_setup = _mfd_clabWIFISSIDTable_undo_setup;
    access_multiplexer->undo_cleanup = _mfd_clabWIFISSIDTable_undo_cleanup;
    access_multiplexer->set_values = _mfd_clabWIFISSIDTable_set_values;
    access_multiplexer->undo_sets = _mfd_clabWIFISSIDTable_undo_values;

    /*
     * no wrappers yet
     */
    access_multiplexer->commit = _mfd_clabWIFISSIDTable_commit;
    access_multiplexer->undo_commit = _mfd_clabWIFISSIDTable_undo_commit;
    access_multiplexer->irreversible_commit = _mfd_clabWIFISSIDTable_irreversible_commit;
#endif

    /*************************************************
     *
     * Create a registration, save our reg data, register table.
     */
    DEBUGMSGTL(("clabWIFISSIDTable:init_clabWIFISSIDTable",
                "Registering clabWIFISSIDTable as a mibs-for-dummies table.\n"));		 
    handler = netsnmp_baby_steps_access_multiplexer_get(access_multiplexer);
    reginfo = netsnmp_handler_registration_create("clabWIFISSIDTable", handler,
                                                  clabWIFISSIDTable_oid,
                                                  clabWIFISSIDTable_oid_size,
                                                  HANDLER_CAN_BABY_STEP |
#if !(defined(NETSNMP_NO_WRITE_SUPPORT) || defined(NETSNMP_DISABLE_SET_SUPPORT))
                                                  HANDLER_CAN_RONLY
#else
                                                  HANDLER_CAN_RWRITE
#endif /* NETSNMP_NO_WRITE_SUPPORT || NETSNMP_DISABLE_SET_SUPPORT */
                                                  );
    if(NULL == reginfo) {
        snmp_log(LOG_ERR,"error registering table clabWIFISSIDTable\n");
        return;
    }
    reginfo->my_reg_void = &clabWIFISSIDTable_if_ctx;

    /*************************************************
     *
     * set up baby steps handler, create it and inject it
     */
    if( access_multiplexer->object_lookup )
        mfd_modes |= BABY_STEP_OBJECT_LOOKUP;
    if( access_multiplexer->pre_request )
        mfd_modes |= BABY_STEP_PRE_REQUEST;
    if( access_multiplexer->post_request )
        mfd_modes |= BABY_STEP_POST_REQUEST;
    
#if !(defined(NETSNMP_NO_WRITE_SUPPORT) || defined(NETSNMP_DISABLE_SET_SUPPORT))
    if( access_multiplexer->set_values )
        mfd_modes |= BABY_STEP_SET_VALUES;
    if( access_multiplexer->irreversible_commit )
        mfd_modes |= BABY_STEP_IRREVERSIBLE_COMMIT;
    if( access_multiplexer->object_syntax_checks )
        mfd_modes |= BABY_STEP_CHECK_OBJECT;

    if( access_multiplexer->undo_setup )
        mfd_modes |= BABY_STEP_UNDO_SETUP;
    if( access_multiplexer->undo_cleanup )
        mfd_modes |= BABY_STEP_UNDO_CLEANUP;
    if( access_multiplexer->undo_sets )
        mfd_modes |= BABY_STEP_UNDO_SETS;
    
    if( access_multiplexer->row_creation )
        mfd_modes |= BABY_STEP_ROW_CREATE;
    if( access_multiplexer->consistency_checks )
        mfd_modes |= BABY_STEP_CHECK_CONSISTENCY;
    if( access_multiplexer->commit )
        mfd_modes |= BABY_STEP_COMMIT;
    if( access_multiplexer->undo_commit )
        mfd_modes |= BABY_STEP_UNDO_COMMIT;
#endif /* NETSNMP_NO_WRITE_SUPPORT || NETSNMP_DISABLE_SET_SUPPORT */
    
    handler = netsnmp_baby_steps_handler_get(mfd_modes);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject row_merge helper with prefix rootoid_len + 2 (entry.col)
     */
    handler = netsnmp_get_row_merge_handler(reginfo->rootoid_len + 2);
    netsnmp_inject_handler(reginfo, handler);

    /*************************************************
     *
     * inject container_table helper
     */
    handler =
        netsnmp_container_table_handler_get(tbl_info,
                                            clabWIFISSIDTable_if_ctx.container,
                                            TABLE_CONTAINER_KEY_NETSNMP_INDEX);
    netsnmp_inject_handler( reginfo, handler );

    /*
     * register table
     */
    netsnmp_register_table(reginfo, tbl_info);

} /* _clabWIFISSIDTable_initialize_interface */

/**
 * @internal
 * Shutdown the table clabWIFISSIDTable
 */
void
_clabWIFISSIDTable_shutdown_interface(clabWIFISSIDTable_registration * reg_ptr)
{
    /*
     * shutdown the container
     */
    _clabWIFISSIDTable_container_shutdown(&clabWIFISSIDTable_if_ctx);
}

void
clabWIFISSIDTable_valid_columns_set(netsnmp_column_info *vc)
{
    clabWIFISSIDTable_if_ctx.tbl_info.valid_columns = vc;
} /* clabWIFISSIDTable_valid_columns_set */

/**
 * @internal
 * convert the index component stored in the context to an oid
 */
int
clabWIFISSIDTable_index_to_oid(netsnmp_index *oid_idx,
                         clabWIFISSIDTable_mib_index *mib_idx)
{
    int err = SNMP_ERR_NOERROR;
    
    /*
     * temp storage for parsing indexes
     */
    /*
     * clabWIFISSIDId(1)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H
     */
    netsnmp_variable_list var_clabWIFISSIDId;

    /*
     * set up varbinds
     */
    memset( &var_clabWIFISSIDId, 0x00, sizeof(var_clabWIFISSIDId) );
    var_clabWIFISSIDId.type = ASN_INTEGER;

    /*
     * chain temp index varbinds together
     */
    var_clabWIFISSIDId.next_variable =  NULL;


    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDTable_index_to_oid","called\n"));

        /* clabWIFISSIDId(1)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H */
    snmp_set_var_value(&var_clabWIFISSIDId, &mib_idx->clabWIFISSIDId, sizeof(mib_idx->clabWIFISSIDId));


    err = build_oid_noalloc(oid_idx->oids, oid_idx->len, &oid_idx->len,
                           NULL, 0, &var_clabWIFISSIDId);
    if(err)
        snmp_log(LOG_ERR,"error %d converting index to oid\n", err);

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers( &var_clabWIFISSIDId );

    return err;
} /* clabWIFISSIDTable_index_to_oid */

/**
 * extract clabWIFISSIDTable indexes from a netsnmp_index
 *
 * @retval SNMP_ERR_NOERROR  : no error
 * @retval SNMP_ERR_GENERR   : error
 */
int
clabWIFISSIDTable_index_from_oid(netsnmp_index *oid_idx,
                         clabWIFISSIDTable_mib_index *mib_idx)
{
    int err = SNMP_ERR_NOERROR;
    
    /*
     * temp storage for parsing indexes
     */
    /*
     * clabWIFISSIDId(1)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H
     */
    netsnmp_variable_list var_clabWIFISSIDId;

    /*
     * set up varbinds
     */
    memset( &var_clabWIFISSIDId, 0x00, sizeof(var_clabWIFISSIDId) );
    var_clabWIFISSIDId.type = ASN_INTEGER;

    /*
     * chain temp index varbinds together
     */
    var_clabWIFISSIDId.next_variable =  NULL;


    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDTable_index_from_oid","called\n"));

    /*
     * parse the oid into the individual index components
     */
    err = parse_oid_indexes( oid_idx->oids, oid_idx->len,
                             &var_clabWIFISSIDId );
    if (err == SNMP_ERR_NOERROR) {
        /*
         * copy out values
         */
    mib_idx->clabWIFISSIDId = *((long *)var_clabWIFISSIDId.val.string);


    }

    /*
     * parsing may have allocated memory. free it.
     */
    snmp_reset_var_buffers( &var_clabWIFISSIDId );

    return err;
} /* clabWIFISSIDTable_index_from_oid */


/*
 * clabWIFISSIDTable_allocate_data
 *
 * Purpose: create new clabWIFISSIDTable_data.
 */
clabWIFISSIDTable_data *
clabWIFISSIDTable_allocate_data(void)
{
    clabWIFISSIDTable_data *rtn = SNMP_MALLOC_TYPEDEF(clabWIFISSIDTable_data);

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDTable_allocate_data","called\n"));

    if(NULL == rtn) {
        snmp_log(LOG_ERR, "unable to malloc memory for new "
                 "clabWIFISSIDTable_data.\n");
    }

    return rtn;
} /* clabWIFISSIDTable_allocate_data */

/*
 * clabWIFISSIDTable_release_data
 *
 * Purpose: release clabWIFISSIDTable data.
 */
void
clabWIFISSIDTable_release_data(clabWIFISSIDTable_data *data)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDTable_release_data","called\n"));

    free(data);
} /* clabWIFISSIDTable_release_data */

/* *********************************************************************
 * @internal
 * allocate resources for a clabWIFISSIDTable_rowreq_ctx
 */
clabWIFISSIDTable_rowreq_ctx *
clabWIFISSIDTable_allocate_rowreq_ctx(void *user_init_ctx)
{
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx =
                  SNMP_MALLOC_TYPEDEF(clabWIFISSIDTable_rowreq_ctx);

    DEBUGMSGTL(("internal:clabWIFISSIDTable:clabWIFISSIDTable_allocate_rowreq_ctx","called\n"));

    if(NULL == rowreq_ctx) {
        snmp_log(LOG_ERR,"Couldn't allocate memory for a "
                 "clabWIFISSIDTable_rowreq_ctx.\n");
        return NULL;
    }

    rowreq_ctx->oid_idx.oids = rowreq_ctx->oid_tmp;

    rowreq_ctx->clabWIFISSIDTable_data_list = NULL;

    /*
     * if we allocated data, call init routine
     */
    if (!(rowreq_ctx->rowreq_flags & MFD_ROW_DATA_FROM_USER)) {
        if(SNMPERR_SUCCESS !=
            clabWIFISSIDTable_rowreq_ctx_init(rowreq_ctx, user_init_ctx)) {
           clabWIFISSIDTable_release_rowreq_ctx(rowreq_ctx);
           rowreq_ctx = NULL;
        }
    }

    return rowreq_ctx;
} /* clabWIFISSIDTable_allocate_rowreq_ctx */

/*
 * @internal
 * release resources for a clabWIFISSIDTable_rowreq_ctx
 */
void
clabWIFISSIDTable_release_rowreq_ctx(clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("internal:clabWIFISSIDTable:clabWIFISSIDTable_release_rowreq_ctx","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    clabWIFISSIDTable_rowreq_ctx_cleanup(rowreq_ctx);

    if(rowreq_ctx->undo)
        clabWIFISSIDTable_release_data(rowreq_ctx->undo);
 
    /*
     * free index oid pointer
     */
    if(rowreq_ctx->oid_idx.oids != rowreq_ctx->oid_tmp)
        free(rowreq_ctx->oid_idx.oids);

    SNMP_FREE(rowreq_ctx);
} /* clabWIFISSIDTable_release_rowreq_ctx */

/**
 * @internal
 * wrapper
 */
static int
_mfd_clabWIFISSIDTable_pre_request(netsnmp_mib_handler *handler,
                            netsnmp_handler_registration *reginfo,
                            netsnmp_agent_request_info *agtreq_info,
                            netsnmp_request_info *requests)
{
    int rc;

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_pre_request",
                "called\n"));
    
    if (1 != netsnmp_row_merge_status_first(reginfo, agtreq_info)) {
        DEBUGMSGTL(("internal:clabWIFISSIDTable",
                    "skipping additional pre_request\n"));
        return SNMP_ERR_NOERROR;
    }
        
    rc = clabWIFISSIDTable_pre_request(clabWIFISSIDTable_if_ctx.user_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("clabWIFISSIDTable","error %d from "
                    "clabWIFISSIDTable_pre_request\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }
    
    return SNMP_ERR_NOERROR;
} /* _mfd_clabWIFISSIDTable_pre_request */

/**
 * @internal
 * wrapper
 */
static int
_mfd_clabWIFISSIDTable_post_request(netsnmp_mib_handler *handler,
                             netsnmp_handler_registration *reginfo,
                             netsnmp_agent_request_info *agtreq_info,
                             netsnmp_request_info *requests)
{
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)
                  netsnmp_container_table_row_extract(requests);
    int rc, packet_rc;

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_post_request",
                "called\n"));

    /*
     * release row context, if deleted
     */
    if (rowreq_ctx && (rowreq_ctx->rowreq_flags & MFD_ROW_DELETED))
        clabWIFISSIDTable_release_rowreq_ctx(rowreq_ctx);

    /*
     * wait for last call before calling user
     */
    if (1 != netsnmp_row_merge_status_last(reginfo, agtreq_info)) {
        DEBUGMSGTL(("internal:clabWIFISSIDTable",
                    "waiting for last post_request\n"));
        return SNMP_ERR_NOERROR;
    }
    
    packet_rc = netsnmp_check_all_requests_error(agtreq_info->asp, 0);
    if ((MFD_SUCCESS != packet_rc) && clabWIFISSIDTable_dirty_get()) {
        /*
         * we shouldn't get here. the undo steps should also clear
         * the dirty flags.
         */
        snmp_log(LOG_WARNING, "clabWIFISSIDTable dirty flag set in post_request "
                 "but status != SUCCESS.\n");
    }

    rc = clabWIFISSIDTable_post_request(clabWIFISSIDTable_if_ctx.user_ctx,packet_rc);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("clabWIFISSIDTable","error %d from "
                    "clabWIFISSIDTable_post_request\n", rc));
    }
    
    return SNMP_ERR_NOERROR;
} /* _mfd_clabWIFISSIDTable_post_request */

NETSNMP_STATIC_INLINE int
_clabWIFISSIDTable_check_indexes(clabWIFISSIDTable_rowreq_ctx * rowreq_ctx)
{
    int                          rc = SNMPERR_SUCCESS;

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_clabWIFISSIDTable_check_indexes","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);


    /* (INDEX) clabWIFISSIDId(1)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H */
    /* check defined range(s). */
    if( (SNMPERR_SUCCESS == rc)
       && ((rowreq_ctx->tbl_idx.clabWIFISSIDId < 1) || (rowreq_ctx->tbl_idx.clabWIFISSIDId > 2147483647))
     ) {
        rc = SNMP_ERR_WRONGVALUE;
    }
       if (MFD_SUCCESS != rc)
          return rc;
        rc = clabWIFISSIDId_check_index( rowreq_ctx );
       if(MFD_SUCCESS != rc)
           return SNMP_ERR_NOCREATION;

    /*
     * if individual parts look ok, check them as a whole
     */
    return clabWIFISSIDTable_validate_index( clabWIFISSIDTable_if_ctx.user_ctx, rowreq_ctx );
} /* _clabWIFISSIDTable_check_indexes */
/**
 * @internal
 * wrapper
 */
static clabWIFISSIDTable_rowreq_ctx *
_mfd_clabWIFISSIDTable_rowreq_from_index(netsnmp_index *oid_idx, int * rc_ptr)
{
    clabWIFISSIDTable_rowreq_ctx * rowreq_ctx;
    clabWIFISSIDTable_mib_index mib_idx;
    int rc;

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_rowreq_from_index",
                "called\n"));

    if (NULL == rc_ptr)
        rc_ptr = &rc;
    *rc_ptr = MFD_SUCCESS;

    memset(&mib_idx, 0x0, sizeof(mib_idx));
    
    /*
     * try to parse oid
     */
    *rc_ptr = clabWIFISSIDTable_index_from_oid(oid_idx, &mib_idx);
    if(MFD_SUCCESS != *rc_ptr) {
        DEBUGMSGT(("clabWIFISSIDTable", "error parsing index\n"));
        return NULL;
    }

    /*
     * allocate new context
     */
    rowreq_ctx = clabWIFISSIDTable_allocate_rowreq_ctx(NULL);
    if (NULL == rowreq_ctx) {
        *rc_ptr = MFD_ERROR;
        return NULL; /* msg already logged */
    }
    
    memcpy(&rowreq_ctx->tbl_idx, &mib_idx, sizeof(mib_idx));

    /*
     * check indexes
     */
    *rc_ptr = _clabWIFISSIDTable_check_indexes(rowreq_ctx);
    if(MFD_SUCCESS != *rc_ptr) {
        netsnmp_assert((*rc_ptr == SNMP_ERR_NOCREATION) ||
                       (*rc_ptr == SNMP_ERR_INCONSISTENTNAME));
        clabWIFISSIDTable_release_rowreq_ctx(rowreq_ctx);
        return NULL;
    }

    /*
     * copy indexes
     */
    rowreq_ctx->oid_idx.len = oid_idx->len;
    memcpy(rowreq_ctx->oid_idx.oids, oid_idx->oids, oid_idx->len * sizeof(oid));

    return rowreq_ctx;
} /* _mfd_clabWIFISSIDTable_rowreq_from_index */


/**
 * @internal
 * wrapper
 */
static int
_mfd_clabWIFISSIDTable_object_lookup(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc = SNMP_ERR_NOERROR;
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)
                  netsnmp_container_table_row_extract(requests);
    
    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_object_lookup","called\n"));

    /*
     * get our context from mfd
     * clabWIFISSIDTable_interface_ctx *if_ctx =
     *             (clabWIFISSIDTable_interface_ctx *)reginfo->my_reg_void;
     */

    if(NULL == rowreq_ctx) {
        netsnmp_table_request_info *tblreq_info;
        netsnmp_index oid_idx;

        tblreq_info = netsnmp_extract_table_info(requests);
        if(NULL == tblreq_info) {
            snmp_log(LOG_ERR, "request had no table info\n");
            return MFD_ERROR;
        }

        /*
         * try create rowreq
         */
        oid_idx.oids = tblreq_info->index_oid;
        oid_idx.len = tblreq_info->index_oid_len;

        rowreq_ctx = _mfd_clabWIFISSIDTable_rowreq_from_index(&oid_idx, &rc);
        if(MFD_SUCCESS == rc) {
            netsnmp_assert(NULL != rowreq_ctx);
            rowreq_ctx->rowreq_flags |= MFD_ROW_CREATED;
            /*
             * add rowreq_ctx to request data lists
             */
            netsnmp_container_table_row_insert(requests, (netsnmp_index*)rowreq_ctx);
        }
    }

    if (MFD_SUCCESS != rc)
        netsnmp_request_set_error_all(requests, rc);
    else
        clabWIFISSIDTable_row_prep(rowreq_ctx);

    return SNMP_VALIDATE_ERR(rc);
} /* _mfd_clabWIFISSIDTable_object_lookup */

/***********************************************************************
 *
 * GET processing
 *
 ***********************************************************************/
/*
 * @internal
 * Retrieve the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_clabWIFISSIDTable_get_column( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_get_column",
                "called for %d\n", column));


    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* clabWIFISSIDEnable(2)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDENABLE:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = clabWIFISSIDEnable_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* clabWIFISSIDStatus(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDSTATUS:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = clabWIFISSIDStatus_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* clabWIFISSIDAlias(4)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDALIAS:
    var->type = ASN_OCTET_STR;
rc = clabWIFISSIDAlias_get(rowreq_ctx, (char **)&var->val.string, &var->val_len );
        break;

    /* clabWIFISSIDName(5)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDNAME:
    var->type = ASN_OCTET_STR;
rc = clabWIFISSIDName_get(rowreq_ctx, (char **)&var->val.string, &var->val_len );
        break;

    /* clabWIFISSIDLastChange(6)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/W/e/r/d/h */
    case COLUMN_CLABWIFISSIDLASTCHANGE:
    var->val_len = sizeof(u_long);
    var->type = ASN_UNSIGNED;
rc = clabWIFISSIDLastChange_get(rowreq_ctx, (u_long *)var->val.string );
        break;

    /* clabWIFISSIDLowerLayers(7)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDLOWERLAYERS:
    var->type = ASN_OCTET_STR;
rc = clabWIFISSIDLowerLayers_get(rowreq_ctx, (char **)&var->val.string, &var->val_len );
        break;

    /* clabWIFISSIDBSSID(8)/MacAddress/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDBSSID:
    var->type = ASN_OCTET_STR;
rc = clabWIFISSIDBSSID_get(rowreq_ctx, (char **)&var->val.string, &var->val_len );
        break;

    /* clabWIFISSIDMACAddress(9)/MacAddress/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDMACADDRESS:
    var->type = ASN_OCTET_STR;
rc = clabWIFISSIDMACAddress_get(rowreq_ctx, (char **)&var->val.string, &var->val_len );
        break;

    /* clabWIFISSIDSSID(10)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDSSID:
    var->type = ASN_OCTET_STR;
rc = clabWIFISSIDSSID_get(rowreq_ctx, (char **)&var->val.string, &var->val_len );
        break;

    /* clabWIFISSIDRowStatus(11)/RowStatus/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDROWSTATUS:
    var->val_len = sizeof(u_long);
    var->type = ASN_INTEGER;
rc = clabWIFISSIDRowStatus_get(rowreq_ctx, (u_long *)var->val.string );
        break;

     default:
        if (CLABWIFISSIDTABLE_MIN_COL <= column && column <= CLABWIFISSIDTABLE_MAX_COL) {
            DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_get_column",
                "assume column %d is reserved\n", column));
            rc = MFD_SKIP;
        } else {
            snmp_log(LOG_ERR,
                "unknown column %d in _clabWIFISSIDTable_get_column\n", column);
        }
        break;
    }

    return rc;
} /* _clabWIFISSIDTable_get_column */

int
_mfd_clabWIFISSIDTable_get_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    u_char                     * old_string;
    void                      (*dataFreeHook)(void *);
    int                        rc;

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_get_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    for(;requests; requests = requests->next) {
        /*
         * save old pointer, so we can free it if replaced
         */
        old_string = requests->requestvb->val.string;
        dataFreeHook = requests->requestvb->dataFreeHook;
        if(NULL == requests->requestvb->val.string) {
            requests->requestvb->val.string = requests->requestvb->buf;
            requests->requestvb->val_len = sizeof(requests->requestvb->buf);
        }
        else if(requests->requestvb->buf == requests->requestvb->val.string) {
            if(requests->requestvb->val_len != sizeof(requests->requestvb->buf))
                requests->requestvb->val_len = sizeof(requests->requestvb->buf);
        }

        /*
         * get column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _clabWIFISSIDTable_get_column(rowreq_ctx, requests->requestvb, tri->colnum);
        if(rc) {
            if(MFD_SKIP == rc) {
                requests->requestvb->type = SNMP_NOSUCHINSTANCE;
                rc = SNMP_ERR_NOERROR;
            }
        }
        else if (NULL == requests->requestvb->val.string) {
            snmp_log(LOG_ERR,"NULL varbind data pointer!\n");
            rc = SNMP_ERR_GENERR;
        }
        if(rc)
            netsnmp_request_set_error(requests, SNMP_VALIDATE_ERR(rc));

        /*
         * if the buffer wasn't used previously for the old data (i.e. it
         * was allcoated memory)  and the get routine replaced the pointer,
         * we need to free the previous pointer.
         */
        if(old_string && (old_string != requests->requestvb->buf) &&
           (requests->requestvb->val.string != old_string)) {
            if(dataFreeHook)
                (*dataFreeHook)(old_string);
            else
                free(old_string);
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_clabWIFISSIDTable_get_values */

/***********************************************************************
 *
 * SET processing
 *
 ***********************************************************************/

/*----------------------------------------------------------------------
 *
 * SET: Syntax checks
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Check the syntax for a particular column
 */
NETSNMP_STATIC_INLINE int
_clabWIFISSIDTable_check_column( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx,
                         netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:clabWIFISSIDTable:_clabWIFISSIDTable_check_column",
                "called for %d\n", column));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {
    /* (INDEX) clabWIFISSIDId(1)/InterfaceIndex/ASN_INTEGER/long(long)//l/a/w/e/R/d/H */
    case COLUMN_CLABWIFISSIDID:
        rc = SNMP_ERR_NOTWRITABLE; /* can not change index of active row */
        break;

    /* clabWIFISSIDEnable(2)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDENABLE:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.clabWIFISSIDEnable ) );
    /* check that the value is one of defined enums */
    if( (SNMPERR_SUCCESS == rc)
 && ( *var->val.integer != TRUTHVALUE_TRUE )
 && ( *var->val.integer != TRUTHVALUE_FALSE )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    if(SNMPERR_SUCCESS != rc) {
        DEBUGMSGTL(("clabWIFISSIDTable:_clabWIFISSIDTable_check_column:clabWIFISSIDEnable",
                    "varbind validation failed (eg bad type or size)\n"));
    }
    else {
        rc = clabWIFISSIDEnable_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from clabWIFISSIDEnable_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* clabWIFISSIDStatus(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDSTATUS:
    rc = netsnmp_check_vb_type_and_size( var, ASN_INTEGER,
        sizeof( rowreq_ctx->data.clabWIFISSIDStatus ) );
    /* check that the value is one of defined enums */
    if( (SNMPERR_SUCCESS == rc)
 && ( *var->val.integer != CLABWIFISSIDSTATUS_UP )
 && ( *var->val.integer != CLABWIFISSIDSTATUS_DOWN )
 && ( *var->val.integer != CLABWIFISSIDSTATUS_UNKNOWN )
 && ( *var->val.integer != CLABWIFISSIDSTATUS_DORMANT )
 && ( *var->val.integer != CLABWIFISSIDSTATUS_NOTPRESENT )
 && ( *var->val.integer != CLABWIFISSIDSTATUS_LOWERLAYERDOWN )
 && ( *var->val.integer != CLABWIFISSIDSTATUS_ERROR )
        ) {
        rc = SNMP_ERR_WRONGVALUE;
        }
    if(SNMPERR_SUCCESS != rc) {
        DEBUGMSGTL(("clabWIFISSIDTable:_clabWIFISSIDTable_check_column:clabWIFISSIDStatus",
                    "varbind validation failed (eg bad type or size)\n"));
    }
    else {
        rc = clabWIFISSIDStatus_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from clabWIFISSIDStatus_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* clabWIFISSIDAlias(4)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDALIAS:
    rc = netsnmp_check_vb_type_and_max_size( var, ASN_OCTET_STR,
        sizeof( rowreq_ctx->data.clabWIFISSIDAlias ) );
    /* check defined range(s). */
    if( (SNMPERR_SUCCESS == rc)
       && ((var->val_len < 0) || (var->val_len > 64))
     ) {
        rc = SNMP_ERR_WRONGLENGTH;
    }
    if(SNMPERR_SUCCESS != rc) {
        DEBUGMSGTL(("clabWIFISSIDTable:_clabWIFISSIDTable_check_column:clabWIFISSIDAlias",
                    "varbind validation failed (eg bad type or size)\n"));
    }
    else {
        rc = clabWIFISSIDAlias_check_value( rowreq_ctx, (char *)var->val.string, var->val_len );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from clabWIFISSIDAlias_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* clabWIFISSIDName(5)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDNAME:
    rc = netsnmp_check_vb_type_and_max_size( var, ASN_OCTET_STR,
        sizeof( rowreq_ctx->data.clabWIFISSIDName ) );
    /* check defined range(s). */
    if( (SNMPERR_SUCCESS == rc)
       && ((var->val_len < 0) || (var->val_len > 64))
     ) {
        rc = SNMP_ERR_WRONGLENGTH;
    }
    if(SNMPERR_SUCCESS != rc) {
        DEBUGMSGTL(("clabWIFISSIDTable:_clabWIFISSIDTable_check_column:clabWIFISSIDName",
                    "varbind validation failed (eg bad type or size)\n"));
    }
    else {
        rc = clabWIFISSIDName_check_value( rowreq_ctx, (char *)var->val.string, var->val_len );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from clabWIFISSIDName_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* clabWIFISSIDLastChange(6)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/W/e/r/d/h */
    case COLUMN_CLABWIFISSIDLASTCHANGE:
    rc = netsnmp_check_vb_type_and_size( var, ASN_UNSIGNED,
        sizeof( rowreq_ctx->data.clabWIFISSIDLastChange ) );
    if(SNMPERR_SUCCESS != rc) {
        DEBUGMSGTL(("clabWIFISSIDTable:_clabWIFISSIDTable_check_column:clabWIFISSIDLastChange",
                    "varbind validation failed (eg bad type or size)\n"));
    }
    else {
        rc = clabWIFISSIDLastChange_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from clabWIFISSIDLastChange_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* clabWIFISSIDLowerLayers(7)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDLOWERLAYERS:
    rc = netsnmp_check_vb_type_and_max_size( var, ASN_OCTET_STR,
        sizeof( rowreq_ctx->data.clabWIFISSIDLowerLayers ) );
    /* check defined range(s). */
    if( (SNMPERR_SUCCESS == rc)
       && ((var->val_len < 0) || (var->val_len > 1024))
     ) {
        rc = SNMP_ERR_WRONGLENGTH;
    }
    if(SNMPERR_SUCCESS != rc) {
        DEBUGMSGTL(("clabWIFISSIDTable:_clabWIFISSIDTable_check_column:clabWIFISSIDLowerLayers",
                    "varbind validation failed (eg bad type or size)\n"));
    }
    else {
        rc = clabWIFISSIDLowerLayers_check_value( rowreq_ctx, (char *)var->val.string, var->val_len );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from clabWIFISSIDLowerLayers_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* clabWIFISSIDBSSID(8)/MacAddress/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDBSSID:
    rc = netsnmp_check_vb_type_and_max_size( var, ASN_OCTET_STR,
        sizeof( rowreq_ctx->data.clabWIFISSIDBSSID ) );
    /* check defined range(s). */
    if( (SNMPERR_SUCCESS == rc)
       && (var->val_len != 6)
     ) {
        rc = SNMP_ERR_WRONGLENGTH;
    }
    if(SNMPERR_SUCCESS != rc) {
        DEBUGMSGTL(("clabWIFISSIDTable:_clabWIFISSIDTable_check_column:clabWIFISSIDBSSID",
                    "varbind validation failed (eg bad type or size)\n"));
    }
    else {
        rc = clabWIFISSIDBSSID_check_value( rowreq_ctx, (char *)var->val.string, var->val_len );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from clabWIFISSIDBSSID_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* clabWIFISSIDMACAddress(9)/MacAddress/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDMACADDRESS:
    rc = netsnmp_check_vb_type_and_max_size( var, ASN_OCTET_STR,
        sizeof( rowreq_ctx->data.clabWIFISSIDMACAddress ) );
    /* check defined range(s). */
    if( (SNMPERR_SUCCESS == rc)
       && (var->val_len != 6)
     ) {
        rc = SNMP_ERR_WRONGLENGTH;
    }
    if(SNMPERR_SUCCESS != rc) {
        DEBUGMSGTL(("clabWIFISSIDTable:_clabWIFISSIDTable_check_column:clabWIFISSIDMACAddress",
                    "varbind validation failed (eg bad type or size)\n"));
    }
    else {
        rc = clabWIFISSIDMACAddress_check_value( rowreq_ctx, (char *)var->val.string, var->val_len );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from clabWIFISSIDMACAddress_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* clabWIFISSIDSSID(10)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDSSID:
    rc = netsnmp_check_vb_type_and_max_size( var, ASN_OCTET_STR,
        sizeof( rowreq_ctx->data.clabWIFISSIDSSID ) );
    /* check defined range(s). */
    if( (SNMPERR_SUCCESS == rc)
       && ((var->val_len < 0) || (var->val_len > 32))
     ) {
        rc = SNMP_ERR_WRONGLENGTH;
    }
    if(SNMPERR_SUCCESS != rc) {
        DEBUGMSGTL(("clabWIFISSIDTable:_clabWIFISSIDTable_check_column:clabWIFISSIDSSID",
                    "varbind validation failed (eg bad type or size)\n"));
    }
    else {
        rc = clabWIFISSIDSSID_check_value( rowreq_ctx, (char *)var->val.string, var->val_len );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from clabWIFISSIDSSID_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

    /* clabWIFISSIDRowStatus(11)/RowStatus/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDROWSTATUS:
rc = netsnmp_check_vb_rowstatus_value(var);
    if(SNMPERR_SUCCESS != rc) {
        DEBUGMSGTL(("clabWIFISSIDTable:_clabWIFISSIDTable_check_column:clabWIFISSIDRowStatus",
                    "varbind validation failed (eg bad type or size)\n"));
    }
    else {
        rc = clabWIFISSIDRowStatus_check_value( rowreq_ctx, *((u_long *)var->val.string) );
        if((MFD_SUCCESS != rc) && (MFD_NOT_VALID_EVER != rc) &&
           (MFD_NOT_VALID_NOW != rc)) {
            snmp_log(LOG_ERR, "bad rc %d from clabWIFISSIDRowStatus_check_value\n", rc);
            rc = SNMP_ERR_GENERR;
        }
    }
        break;

        default: /** We shouldn't get here */
            rc = SNMP_ERR_GENERR;
            snmp_log(LOG_ERR, "unknown column %d in _clabWIFISSIDTable_check_column\n", column);
    }

    return rc;
} /* _clabWIFISSIDTable_check_column */

int
_mfd_clabWIFISSIDTable_check_objects(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    int                          rc;

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_check_objects","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    for(;requests; requests = requests->next) {

        /*
         * get column number from table request info, and check that column
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;

        rc = _clabWIFISSIDTable_check_column(rowreq_ctx, requests->requestvb, tri->colnum);
        if(rc) {
            netsnmp_request_set_error(requests, SNMP_VALIDATE_ERR(rc));
            break;
        }

    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_clabWIFISSIDTable_check_objects */


/*----------------------------------------------------------------------
 *
 * SET: Undo setup
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Set the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_clabWIFISSIDTable_undo_setup_column( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:clabWIFISSIDTable:_clabWIFISSIDTable_undo_setup_column",
                "called for %d\n", column));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* clabWIFISSIDEnable(2)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDENABLE:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDENABLE_FLAG;
        rc = clabWIFISSIDEnable_undo_setup(rowreq_ctx );
        break;

    /* clabWIFISSIDStatus(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDSTATUS:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDSTATUS_FLAG;
        rc = clabWIFISSIDStatus_undo_setup(rowreq_ctx );
        break;

    /* clabWIFISSIDAlias(4)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDALIAS:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDALIAS_FLAG;
        rc = clabWIFISSIDAlias_undo_setup(rowreq_ctx );
        break;

    /* clabWIFISSIDName(5)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDNAME:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDNAME_FLAG;
        rc = clabWIFISSIDName_undo_setup(rowreq_ctx );
        break;

    /* clabWIFISSIDLastChange(6)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/W/e/r/d/h */
    case COLUMN_CLABWIFISSIDLASTCHANGE:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDLASTCHANGE_FLAG;
        rc = clabWIFISSIDLastChange_undo_setup(rowreq_ctx );
        break;

    /* clabWIFISSIDLowerLayers(7)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDLOWERLAYERS:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDLOWERLAYERS_FLAG;
        rc = clabWIFISSIDLowerLayers_undo_setup(rowreq_ctx );
        break;

    /* clabWIFISSIDBSSID(8)/MacAddress/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDBSSID:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDBSSID_FLAG;
        rc = clabWIFISSIDBSSID_undo_setup(rowreq_ctx );
        break;

    /* clabWIFISSIDMACAddress(9)/MacAddress/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDMACADDRESS:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDMACADDRESS_FLAG;
        rc = clabWIFISSIDMACAddress_undo_setup(rowreq_ctx );
        break;

    /* clabWIFISSIDSSID(10)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDSSID:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDSSID_FLAG;
        rc = clabWIFISSIDSSID_undo_setup(rowreq_ctx );
        break;

    /* clabWIFISSIDRowStatus(11)/RowStatus/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDROWSTATUS:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDROWSTATUS_FLAG;
        rc = clabWIFISSIDRowStatus_undo_setup(rowreq_ctx );
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _clabWIFISSIDTable_undo_setup_column\n", column);
         break;
    }

    return rc;
} /* _clabWIFISSIDTable_undo_setup_column */


/**
 * @internal
 * undo setup
 */
int
_mfd_clabWIFISSIDTable_undo_setup(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_undo_setup","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * allocate undo context
     */
    rowreq_ctx->undo = clabWIFISSIDTable_allocate_data();
    if(NULL == rowreq_ctx->undo) {
        /** msg already logged */
        netsnmp_request_set_error_all(requests, SNMP_ERR_RESOURCEUNAVAILABLE);
        return SNMP_ERR_NOERROR;
    }

    /*
     * row undo setup
     */
    rowreq_ctx->column_set_flags = 0;
    rc = clabWIFISSIDTable_undo_setup(rowreq_ctx);
    if (MFD_SUCCESS != rc)  {
        DEBUGMSGTL(("clabWIFISSIDTable:mfd","error %d from "
                    "clabWIFISSIDTable_undo_setup\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }
    else {
        /*
         * column undo setup
         */
        netsnmp_table_request_info * tri;
        for(;requests; requests = requests->next) {
            /*
             * set column data
             */
            tri = netsnmp_extract_table_info(requests);
            if(NULL == tri)
                continue;
            
            rc = _clabWIFISSIDTable_undo_setup_column(rowreq_ctx, tri->colnum);
            if(MFD_SUCCESS != rc)  {
                DEBUGMSGTL(("clabWIFISSIDTable:mfd","error %d from "
                            "clabWIFISSIDTable_undo_setup_column\n", rc));
                netsnmp_set_request_error(agtreq_info, requests, SNMP_VALIDATE_ERR(rc));
            }
        } /* for results */
    }
    
    return SNMP_ERR_NOERROR;
} /* _mfd_clabWIFISSIDTable_undo_setup */

/**
 * @internal
 * undo setup
 */
int
_mfd_clabWIFISSIDTable_undo_cleanup(netsnmp_mib_handler *handler,
                             netsnmp_handler_registration *reginfo,
                             netsnmp_agent_request_info *agtreq_info,
                             netsnmp_request_info *requests)
{
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)
                  netsnmp_container_table_row_extract(requests);
    int rc;

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_undo_cleanup","called\n"));

    /*
     * failed row create in early stages has no rowreq_ctx
     */
    if (NULL == rowreq_ctx)
        return MFD_SUCCESS;

    /*
     * call user cleanup
     */
    rc = clabWIFISSIDTable_undo_cleanup(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("clabWIFISSIDTable:mfd","error %d from "
                    "clabWIFISSIDTable_undo_cleanup\n", rc));
    }

    /*
     * release undo context, if needed
     */
    if(rowreq_ctx->undo) {
         clabWIFISSIDTable_release_data(rowreq_ctx->undo);
         rowreq_ctx->undo = NULL;
    }


    return SNMP_ERR_NOERROR;
} /* _mfd_clabWIFISSIDTable_undo_cleanup */
#endif
/*----------------------------------------------------------------------
 *
 * SET: Set values
 *
 *---------------------------------------------------------------------*/
/*
 * @internal
 * Set the value for a particular column
 */
int
_clabWIFISSIDTable_set_column( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:clabWIFISSIDTable:_clabWIFISSIDTable_set_column",
                "called for %d\n", column));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* clabWIFISSIDEnable(2)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDENABLE:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDENABLE_FLAG;
        rc = clabWIFISSIDEnable_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* clabWIFISSIDStatus(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDSTATUS:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDSTATUS_FLAG;
        rc = clabWIFISSIDStatus_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* clabWIFISSIDAlias(4)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDALIAS:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDALIAS_FLAG;
        rc = clabWIFISSIDAlias_set(rowreq_ctx, (char *)var->val.string, var->val_len );
        break;

    /* clabWIFISSIDName(5)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDNAME:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDNAME_FLAG;
        rc = clabWIFISSIDName_set(rowreq_ctx, (char *)var->val.string, var->val_len );
        break;

    /* clabWIFISSIDLastChange(6)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/W/e/r/d/h */
    case COLUMN_CLABWIFISSIDLASTCHANGE:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDLASTCHANGE_FLAG;
        rc = clabWIFISSIDLastChange_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

    /* clabWIFISSIDLowerLayers(7)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDLOWERLAYERS:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDLOWERLAYERS_FLAG;
        rc = clabWIFISSIDLowerLayers_set(rowreq_ctx, (char *)var->val.string, var->val_len );
        break;

    /* clabWIFISSIDBSSID(8)/MacAddress/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDBSSID:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDBSSID_FLAG;
        rc = clabWIFISSIDBSSID_set(rowreq_ctx, (char *)var->val.string, var->val_len );
        break;

    /* clabWIFISSIDMACAddress(9)/MacAddress/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDMACADDRESS:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDMACADDRESS_FLAG;
        rc = clabWIFISSIDMACAddress_set(rowreq_ctx, (char *)var->val.string, var->val_len );
        break;

    /* clabWIFISSIDSSID(10)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDSSID:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDSSID_FLAG;
        rc = clabWIFISSIDSSID_set(rowreq_ctx, (char *)var->val.string, var->val_len );
        break;

    /* clabWIFISSIDRowStatus(11)/RowStatus/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDROWSTATUS:
        rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDROWSTATUS_FLAG;
        rc = clabWIFISSIDRowStatus_set(rowreq_ctx, *((u_long *)var->val.string) );
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _clabWIFISSIDTable_set_column\n", column);
         rc = SNMP_ERR_GENERR;
         break;
    }
    
    return rc;
} /* _clabWIFISSIDTable_set_column */
#if 0
int
_mfd_clabWIFISSIDTable_set_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;
    int                          rc = SNMP_ERR_NOERROR;

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_set_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    rowreq_ctx->column_set_flags = 0;
    for(;requests; requests = requests->next) {
        /*
         * set column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _clabWIFISSIDTable_set_column(rowreq_ctx,
                                    requests->requestvb, tri->colnum);
        if(MFD_SUCCESS != rc)  {
            DEBUGMSGTL(("clabWIFISSIDTable:mfd","error %d from "
                        "clabWIFISSIDTable_set_column\n", rc));
            netsnmp_set_request_error(agtreq_info, requests, SNMP_VALIDATE_ERR(rc));
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_clabWIFISSIDTable_set_values */

/*----------------------------------------------------------------------
 *
 * SET: commit
 *
 *---------------------------------------------------------------------*/
/**
 * @internal
 * commit the values
 */
int
_mfd_clabWIFISSIDTable_commit(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_commit","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    rc = clabWIFISSIDTable_commit(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        DEBUGMSGTL(("clabWIFISSIDTable:mfd","error %d from "
                    "clabWIFISSIDTable_commit\n", rc));
        netsnmp_request_set_error_all(requests, SNMP_VALIDATE_ERR(rc));
    }

    if (rowreq_ctx->rowreq_flags & MFD_ROW_DIRTY) {
        /*
         * if we successfully commited this row, set the dirty flag. Use the
         * current value + 1 (i.e. dirty = # rows changed).
         * this is checked in post_request...
         */
        clabWIFISSIDTable_dirty_set( clabWIFISSIDTable_dirty_get() + 1 ); /* set table dirty flag */
    }

    return SNMP_ERR_NOERROR;
}

int
_mfd_clabWIFISSIDTable_undo_commit(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)
                  netsnmp_container_table_row_extract(requests);

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_undo_commit","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    if (rowreq_ctx->rowreq_flags & MFD_ROW_DIRTY) {
        u_int  d = clabWIFISSIDTable_dirty_get();

        netsnmp_assert(d != 0);
        if(d)
            clabWIFISSIDTable_dirty_set( d - 1 );
    }

    rc = clabWIFISSIDTable_undo_commit(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("clabWIFISSIDTable:mfd","error %d from "
                    "clabWIFISSIDTable_undo_commit\n", rc));
    }

    if (rowreq_ctx->rowreq_flags & MFD_ROW_DIRTY) {
        snmp_log(LOG_WARNING, "clabWIFISSIDTable row dirty flag still set after undo_commit\n");
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_clabWIFISSIDTable_commit */

/*----------------------------------------------------------------------
 *
 * SET: Undo
 *
 *---------------------------------------------------------------------*/
/**
 * @internal
 * undo the value for a particular column
 */
NETSNMP_STATIC_INLINE int
_clabWIFISSIDTable_undo_column( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx,
                       netsnmp_variable_list *var, int column )
{
    int rc = SNMPERR_SUCCESS;
    
    DEBUGMSGTL(("internal:clabWIFISSIDTable:_clabWIFISSIDTable_undo_column",
                "called for %d\n", column));

    netsnmp_assert(NULL != rowreq_ctx);

    switch(column) {

    /* clabWIFISSIDEnable(2)/TruthValue/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDENABLE:
        rc = clabWIFISSIDEnable_undo(rowreq_ctx);
        break;

    /* clabWIFISSIDStatus(3)/INTEGER/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDSTATUS:
        rc = clabWIFISSIDStatus_undo(rowreq_ctx);
        break;

    /* clabWIFISSIDAlias(4)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDALIAS:
        rc = clabWIFISSIDAlias_undo(rowreq_ctx);
        break;

    /* clabWIFISSIDName(5)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDNAME:
        rc = clabWIFISSIDName_undo(rowreq_ctx);
        break;

    /* clabWIFISSIDLastChange(6)/UNSIGNED32/ASN_UNSIGNED/u_long(u_long)//l/A/W/e/r/d/h */
    case COLUMN_CLABWIFISSIDLASTCHANGE:
        rc = clabWIFISSIDLastChange_undo(rowreq_ctx);
        break;

    /* clabWIFISSIDLowerLayers(7)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDLOWERLAYERS:
        rc = clabWIFISSIDLowerLayers_undo(rowreq_ctx);
        break;

    /* clabWIFISSIDBSSID(8)/MacAddress/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDBSSID:
        rc = clabWIFISSIDBSSID_undo(rowreq_ctx);
        break;

    /* clabWIFISSIDMACAddress(9)/MacAddress/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDMACADDRESS:
        rc = clabWIFISSIDMACAddress_undo(rowreq_ctx);
        break;

    /* clabWIFISSIDSSID(10)/SnmpAdminString/ASN_OCTET_STR/char(char)//L/A/W/e/R/d/H */
    case COLUMN_CLABWIFISSIDSSID:
        rc = clabWIFISSIDSSID_undo(rowreq_ctx);
        break;

    /* clabWIFISSIDRowStatus(11)/RowStatus/ASN_INTEGER/long(u_long)//l/A/W/E/r/d/h */
    case COLUMN_CLABWIFISSIDROWSTATUS:
        rc = clabWIFISSIDRowStatus_undo(rowreq_ctx);
        break;

     default:
         snmp_log(LOG_ERR,"unknown column %d in _clabWIFISSIDTable_undo_column\n", column);
         break;
    }

    return rc;
} /* _clabWIFISSIDTable_undo_column */

int
_mfd_clabWIFISSIDTable_undo_values(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    int                    rc;
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)
                  netsnmp_container_table_row_extract(requests);
    netsnmp_table_request_info * tri;

    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_undo_values","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    rc = clabWIFISSIDTable_undo(rowreq_ctx);
    if (MFD_SUCCESS != rc) {
        /*
         * nothing we can do about it but log it
         */
        DEBUGMSGTL(("clabWIFISSIDTable:mfd","error %d from "
                    "clabWIFISSIDTable_undo\n", rc));
    }
    
    for(;requests; requests = requests->next) {
        /*
         * set column data
         */
        tri = netsnmp_extract_table_info(requests);
        if(NULL == tri)
            continue;
        
        rc = _clabWIFISSIDTable_undo_column(rowreq_ctx, requests->requestvb,
                                     tri->colnum);
        if (MFD_SUCCESS != rc) {
            /*
             * nothing we can do about it but log it
             */
            DEBUGMSGTL(("clabWIFISSIDTable:mfd","error %d from "
                        "clabWIFISSIDTable_undo_column\n", rc));
        }
    } /* for results */

    return SNMP_ERR_NOERROR;
} /* _mfd_clabWIFISSIDTable_undo_values */

/*----------------------------------------------------------------------
 *
 * SET: irreversible commit
 *
 *---------------------------------------------------------------------*/
/**
 * @internal
 * commit irreversible actions
 */
int
_mfd_clabWIFISSIDTable_irreversible_commit(netsnmp_mib_handler *handler,
                         netsnmp_handler_registration *reginfo,
                         netsnmp_agent_request_info *agtreq_info,
                         netsnmp_request_info *requests)
{
    clabWIFISSIDTable_rowreq_ctx *rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)
                  netsnmp_container_table_row_extract(requests);
    
    DEBUGMSGTL(("internal:clabWIFISSIDTable:_mfd_clabWIFISSIDTable_irreversible:commit","called\n"));
   
    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * check for and handle row creation/deletion
     * and update column exist flags...
     */
    if (rowreq_ctx->rowreq_flags & MFD_ROW_DELETED) {
        if (! (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED))
            CONTAINER_REMOVE(clabWIFISSIDTable_if_ctx.container, rowreq_ctx);
    }
    else {
        if (rowreq_ctx->column_set_flags) {
           rowreq_ctx->column_set_flags = 0;
        }
        if (rowreq_ctx->rowreq_flags & MFD_ROW_CREATED) {
            rowreq_ctx->rowreq_flags &= ~MFD_ROW_CREATED;
            CONTAINER_INSERT(clabWIFISSIDTable_if_ctx.container, rowreq_ctx);
        }
    }

    return SNMP_ERR_NOERROR;
} /* _mfd_clabWIFISSIDTable_irreversible_commit */

/***********************************************************************
 *
 * DATA ACCESS
 *
 ***********************************************************************/
/**
 * @internal
 */
static void
_container_item_free(clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, void *context)
{
    DEBUGMSGTL(("internal:clabWIFISSIDTable:_container_item_free","called\n"));

    if(NULL == rowreq_ctx)
        return;

    clabWIFISSIDTable_release_rowreq_ctx(rowreq_ctx);
} /* _container_item_free */

/**
 * @internal
 */
static void
_container_free(netsnmp_container *container)
{
    DEBUGMSGTL(("internal:clabWIFISSIDTable:_container_free","called\n"));

    if (NULL == container) {
        snmp_log(LOG_ERR, "invalid container in clabWIFISSIDTable_container_free\n");
        return;
    }

    /*
     * call user code
     */
    clabWIFISSIDTable_container_free(container);
    
    /*
     * free all items. inefficient, but easy.
     */
    CONTAINER_CLEAR(container,
                    (netsnmp_container_obj_func *)_container_item_free,
                    NULL);
} /* _container_free */

/**
 * @internal
 * initialize the container with functions or wrappers
 */
void
_clabWIFISSIDTable_container_init(clabWIFISSIDTable_interface_ctx *if_ctx)
{
    DEBUGMSGTL(("internal:clabWIFISSIDTable:_clabWIFISSIDTable_container_init","called\n"));

    /*
     * container init
     */    
    clabWIFISSIDTable_container_init(&if_ctx->container);
    if(NULL == if_ctx->container)
        if_ctx->container = netsnmp_container_find("clabWIFISSIDTable:table_container");
    if(NULL == if_ctx->container) {
        snmp_log(LOG_ERR,"error creating container in "
                 "clabWIFISSIDTable_container_init\n");
        return;
    }

} /* _clabWIFISSIDTable_container_init */

/**
 * @internal
 * shutdown the container with functions or wrappers
 */
void
_clabWIFISSIDTable_container_shutdown(clabWIFISSIDTable_interface_ctx *if_ctx)
{
    DEBUGMSGTL(("internal:clabWIFISSIDTable:_clabWIFISSIDTable_container_shutdown","called\n"));

    clabWIFISSIDTable_container_shutdown(if_ctx->container);

    _container_free(if_ctx->container);

} /* _clabWIFISSIDTable_container_shutdown */


clabWIFISSIDTable_rowreq_ctx *
clabWIFISSIDTable_row_find_by_mib_index(clabWIFISSIDTable_mib_index *mib_idx)
{
    clabWIFISSIDTable_rowreq_ctx   *rowreq_ctx;
    oid                      oid_tmp[MAX_OID_LEN];
    netsnmp_index            oid_idx;
    int                      rc;

    /*
     * set up storage for OID
     */
    oid_idx.oids = oid_tmp;
    oid_idx.len = sizeof(oid_tmp)/sizeof(oid);

    /*
     * convert
     */
    rc = clabWIFISSIDTable_index_to_oid(&oid_idx, mib_idx);
    if (MFD_SUCCESS != rc)
        return NULL;

    rowreq_ctx = (clabWIFISSIDTable_rowreq_ctx*)CONTAINER_FIND(clabWIFISSIDTable_if_ctx.container, &oid_idx);

    return rowreq_ctx;
}
#endif

