/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "clabWIFISSIDTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table clabWIFISSIDTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * CLAB-WIFI-MIB::clabWIFISSIDTable is subid 4 of clabWIFIObjects.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     * +-------------+        +==============+
     * |    row    |f|<-------||  object    ||
     * |  create   |1|      E ||  lookup    ||
     * +-------------+        +==============+
     *     E |   | S                 | S
     *       |   +------------------>|
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                               |
     *                          (err && f1)------------------->+
     *                               |                         |
     *                        +--------------+         +--------------+
     *                        |    post      |<--------|      row     |
     *                        |   request    |       U |    release   |
     *                        +--------------+         +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * clabWIFISSIDTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * clabWIFISSIDTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
clabWIFISSIDTable_undo_setup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup clabWIFISSIDTable undo.
     * set up clabWIFISSIDTable undo information, in preparation for a set.
     * Undo storage is in (* clabWIFISSIDRowStatus_val_ptr )*
     */

    return rc;
} /* clabWIFISSIDTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
clabWIFISSIDTable_undo( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> clabWIFISSIDTable undo.
     * clabWIFISSIDTable undo information, in response to a failed set.
     * Undo storage is in (* clabWIFISSIDRowStatus_val_ptr )*
     */

    return rc;
} /* clabWIFISSIDTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
clabWIFISSIDTable_undo_cleanup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup clabWIFISSIDTable undo.
     * Undo storage is in (* clabWIFISSIDRowStatus_val_ptr )*
     */

    return rc;
} /* clabWIFISSIDTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * clabWIFISSIDTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param clabWIFISSIDTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
clabWIFISSIDTable_commit( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit clabWIFISSIDTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_CLABWIFISSIDENABLE_FLAG) {
       save_flags &= ~COLUMN_CLABWIFISSIDENABLE_FLAG; /* clear clabWIFISSIDEnable */
       /*
        * TODO:482:o: |-> commit column clabWIFISSIDEnable.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"clabWIFISSIDTable column clabWIFISSIDEnable commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo clabWIFISSIDEnable
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDENABLE_FLAG;
       }
    }

    if (save_flags & COLUMN_CLABWIFISSIDSTATUS_FLAG) {
       save_flags &= ~COLUMN_CLABWIFISSIDSTATUS_FLAG; /* clear clabWIFISSIDStatus */
       /*
        * TODO:482:o: |-> commit column clabWIFISSIDStatus.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"clabWIFISSIDTable column clabWIFISSIDStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo clabWIFISSIDStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDSTATUS_FLAG;
       }
    }

    if (save_flags & COLUMN_CLABWIFISSIDALIAS_FLAG) {
       save_flags &= ~COLUMN_CLABWIFISSIDALIAS_FLAG; /* clear clabWIFISSIDAlias */
       /*
        * TODO:482:o: |-> commit column clabWIFISSIDAlias.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"clabWIFISSIDTable column clabWIFISSIDAlias commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo clabWIFISSIDAlias
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDALIAS_FLAG;
       }
    }

    if (save_flags & COLUMN_CLABWIFISSIDNAME_FLAG) {
       save_flags &= ~COLUMN_CLABWIFISSIDNAME_FLAG; /* clear clabWIFISSIDName */
       /*
        * TODO:482:o: |-> commit column clabWIFISSIDName.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"clabWIFISSIDTable column clabWIFISSIDName commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo clabWIFISSIDName
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDNAME_FLAG;
       }
    }

    if (save_flags & COLUMN_CLABWIFISSIDLASTCHANGE_FLAG) {
       save_flags &= ~COLUMN_CLABWIFISSIDLASTCHANGE_FLAG; /* clear clabWIFISSIDLastChange */
       /*
        * TODO:482:o: |-> commit column clabWIFISSIDLastChange.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"clabWIFISSIDTable column clabWIFISSIDLastChange commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo clabWIFISSIDLastChange
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDLASTCHANGE_FLAG;
       }
    }

    if (save_flags & COLUMN_CLABWIFISSIDLOWERLAYERS_FLAG) {
       save_flags &= ~COLUMN_CLABWIFISSIDLOWERLAYERS_FLAG; /* clear clabWIFISSIDLowerLayers */
       /*
        * TODO:482:o: |-> commit column clabWIFISSIDLowerLayers.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"clabWIFISSIDTable column clabWIFISSIDLowerLayers commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo clabWIFISSIDLowerLayers
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDLOWERLAYERS_FLAG;
       }
    }

    if (save_flags & COLUMN_CLABWIFISSIDBSSID_FLAG) {
       save_flags &= ~COLUMN_CLABWIFISSIDBSSID_FLAG; /* clear clabWIFISSIDBSSID */
       /*
        * TODO:482:o: |-> commit column clabWIFISSIDBSSID.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"clabWIFISSIDTable column clabWIFISSIDBSSID commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo clabWIFISSIDBSSID
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDBSSID_FLAG;
       }
    }

    if (save_flags & COLUMN_CLABWIFISSIDMACADDRESS_FLAG) {
       save_flags &= ~COLUMN_CLABWIFISSIDMACADDRESS_FLAG; /* clear clabWIFISSIDMACAddress */
       /*
        * TODO:482:o: |-> commit column clabWIFISSIDMACAddress.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"clabWIFISSIDTable column clabWIFISSIDMACAddress commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo clabWIFISSIDMACAddress
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDMACADDRESS_FLAG;
       }
    }

    if (save_flags & COLUMN_CLABWIFISSIDSSID_FLAG) {
       save_flags &= ~COLUMN_CLABWIFISSIDSSID_FLAG; /* clear clabWIFISSIDSSID */
       /*
        * TODO:482:o: |-> commit column clabWIFISSIDSSID.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"clabWIFISSIDTable column clabWIFISSIDSSID commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo clabWIFISSIDSSID
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDSSID_FLAG;
       }
    }

    if (save_flags & COLUMN_CLABWIFISSIDROWSTATUS_FLAG) {
       save_flags &= ~COLUMN_CLABWIFISSIDROWSTATUS_FLAG; /* clear clabWIFISSIDRowStatus */
       /*
        * TODO:482:o: |-> commit column clabWIFISSIDRowStatus.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"clabWIFISSIDTable column clabWIFISSIDRowStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo clabWIFISSIDRowStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLABWIFISSIDROWSTATUS_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* clabWIFISSIDTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * clabWIFISSIDTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param clabWIFISSIDTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
clabWIFISSIDTable_undo_commit( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo clabWIFISSIDTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* clabWIFISSIDTable_undo_commit */

/*
 * TODO:440:M: Implement clabWIFISSIDTable node value checks.
 * TODO:450:M: Implement clabWIFISSIDTable undo functions.
 * TODO:460:M: Implement clabWIFISSIDTable set functions.
 * TODO:480:M: Implement clabWIFISSIDTable commit functions.
 */
/*---------------------------------------------------------------------
 * CLAB-WIFI-MIB::clabWIFISSIDEntry.clabWIFISSIDEnable
 * clabWIFISSIDEnable is subid 2 of clabWIFISSIDEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4.1.2
 * Description:
This object is defined in TR-181 Device.WiFi.SSID{i}.Enable.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param clabWIFISSIDEnable_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * clabWIFISSIDTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
clabWIFISSIDEnable_check_value( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, u_long clabWIFISSIDEnable_val)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDEnable_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid clabWIFISSIDEnable value.
     */

    return MFD_SUCCESS; /* clabWIFISSIDEnable value not illegal */
} /* clabWIFISSIDEnable_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * clabWIFISSIDTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
clabWIFISSIDEnable_undo_setup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDEnable_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup clabWIFISSIDEnable undo.
     */
    /*
     * copy clabWIFISSIDEnable data
     * set rowreq_ctx->undo->clabWIFISSIDEnable from rowreq_ctx->data.clabWIFISSIDEnable
     */
    rowreq_ctx->undo->clabWIFISSIDEnable = rowreq_ctx->data.clabWIFISSIDEnable;


    return MFD_SUCCESS;
} /* clabWIFISSIDEnable_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param clabWIFISSIDEnable_val
 *        A long containing the new value.
 */
int
clabWIFISSIDEnable_set( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, u_long clabWIFISSIDEnable_val )
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDEnable_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set clabWIFISSIDEnable value.
     * set clabWIFISSIDEnable value in rowreq_ctx->data
     */
    rowreq_ctx->data.clabWIFISSIDEnable = clabWIFISSIDEnable_val;

    return MFD_SUCCESS;
} /* clabWIFISSIDEnable_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
clabWIFISSIDEnable_undo( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDEnable_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up clabWIFISSIDEnable undo.
     */
    /*
     * copy clabWIFISSIDEnable data
     * set rowreq_ctx->data.clabWIFISSIDEnable from rowreq_ctx->undo->clabWIFISSIDEnable
     */
    rowreq_ctx->data.clabWIFISSIDEnable = rowreq_ctx->undo->clabWIFISSIDEnable;

    
    return MFD_SUCCESS;
} /* clabWIFISSIDEnable_undo */

/*---------------------------------------------------------------------
 * CLAB-WIFI-MIB::clabWIFISSIDEntry.clabWIFISSIDStatus
 * clabWIFISSIDStatus is subid 3 of clabWIFISSIDEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4.1.3
 * Description:
This object is defined in TR-181 Device.WiFi.SSID{i}.Status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 4/8. Values:  up(1), down(2), unknown(4), dormant(5), notPresent(6), lowerLayerDown(7), error(8)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param clabWIFISSIDStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * clabWIFISSIDTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  up(1), down(2), unknown(4), dormant(5), notPresent(6), lowerLayerDown(7), error(8)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
clabWIFISSIDStatus_check_value( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, u_long clabWIFISSIDStatus_val)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid clabWIFISSIDStatus value.
     */

    return MFD_SUCCESS; /* clabWIFISSIDStatus value not illegal */
} /* clabWIFISSIDStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * clabWIFISSIDTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
clabWIFISSIDStatus_undo_setup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup clabWIFISSIDStatus undo.
     */
    /*
     * copy clabWIFISSIDStatus data
     * set rowreq_ctx->undo->clabWIFISSIDStatus from rowreq_ctx->data.clabWIFISSIDStatus
     */
    rowreq_ctx->undo->clabWIFISSIDStatus = rowreq_ctx->data.clabWIFISSIDStatus;


    return MFD_SUCCESS;
} /* clabWIFISSIDStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param clabWIFISSIDStatus_val
 *        A long containing the new value.
 */
int
clabWIFISSIDStatus_set( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, u_long clabWIFISSIDStatus_val )
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set clabWIFISSIDStatus value.
     * set clabWIFISSIDStatus value in rowreq_ctx->data
     */
    rowreq_ctx->data.clabWIFISSIDStatus = clabWIFISSIDStatus_val;

    return MFD_SUCCESS;
} /* clabWIFISSIDStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
clabWIFISSIDStatus_undo( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up clabWIFISSIDStatus undo.
     */
    /*
     * copy clabWIFISSIDStatus data
     * set rowreq_ctx->data.clabWIFISSIDStatus from rowreq_ctx->undo->clabWIFISSIDStatus
     */
    rowreq_ctx->data.clabWIFISSIDStatus = rowreq_ctx->undo->clabWIFISSIDStatus;

    
    return MFD_SUCCESS;
} /* clabWIFISSIDStatus_undo */

/*---------------------------------------------------------------------
 * CLAB-WIFI-MIB::clabWIFISSIDEntry.clabWIFISSIDAlias
 * clabWIFISSIDAlias is subid 4 of clabWIFISSIDEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4.1.4
 * Description:
This object is defined in TR-181 Device.WiFi.SSID{i}.Alias.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255t
 *
 * Ranges:  0 - 64;
 *
 * Its syntax is SnmpAdminString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 64)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param clabWIFISSIDAlias_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDAlias_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDAlias_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * clabWIFISSIDTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.clabWIFISSIDAlias).
 *    The length is in (one of) the range set(s):  0 - 64
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
clabWIFISSIDAlias_check_value( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDAlias_val_ptr,  size_t clabWIFISSIDAlias_val_ptr_len)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDAlias_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDAlias_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid clabWIFISSIDAlias value.
     */

    return MFD_SUCCESS; /* clabWIFISSIDAlias value not illegal */
} /* clabWIFISSIDAlias_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * clabWIFISSIDTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
clabWIFISSIDAlias_undo_setup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDAlias_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup clabWIFISSIDAlias undo.
     */
    /*
     * copy clabWIFISSIDAlias and clabWIFISSIDAlias_len data
     * set rowreq_ctx->undo->clabWIFISSIDAlias from rowreq_ctx->data.clabWIFISSIDAlias
     */
    memcpy( rowreq_ctx->undo->clabWIFISSIDAlias, rowreq_ctx->data.clabWIFISSIDAlias,
            (rowreq_ctx->data.clabWIFISSIDAlias_len * sizeof(rowreq_ctx->undo->clabWIFISSIDAlias[0])));
    rowreq_ctx->undo->clabWIFISSIDAlias_len = rowreq_ctx->data.clabWIFISSIDAlias_len;


    return MFD_SUCCESS;
} /* clabWIFISSIDAlias_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param clabWIFISSIDAlias_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDAlias_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDAlias_val_ptr
 */
int
clabWIFISSIDAlias_set( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDAlias_val_ptr,  size_t clabWIFISSIDAlias_val_ptr_len )
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDAlias_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDAlias_val_ptr);

    /*
     * TODO:461:M: |-> Set clabWIFISSIDAlias value.
     * set clabWIFISSIDAlias value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDAlias, clabWIFISSIDAlias_val_ptr, clabWIFISSIDAlias_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.clabWIFISSIDAlias_len = clabWIFISSIDAlias_val_ptr_len / sizeof(clabWIFISSIDAlias_val_ptr[0]);

    return MFD_SUCCESS;
} /* clabWIFISSIDAlias_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
clabWIFISSIDAlias_undo( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDAlias_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up clabWIFISSIDAlias undo.
     */
    /*
     * copy clabWIFISSIDAlias and clabWIFISSIDAlias_len data
     * set rowreq_ctx->data.clabWIFISSIDAlias from rowreq_ctx->undo->clabWIFISSIDAlias
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDAlias, rowreq_ctx->undo->clabWIFISSIDAlias,
            (rowreq_ctx->undo->clabWIFISSIDAlias_len * sizeof(rowreq_ctx->data.clabWIFISSIDAlias[0])));
    rowreq_ctx->data.clabWIFISSIDAlias_len = rowreq_ctx->undo->clabWIFISSIDAlias_len;

    
    return MFD_SUCCESS;
} /* clabWIFISSIDAlias_undo */

/*---------------------------------------------------------------------
 * CLAB-WIFI-MIB::clabWIFISSIDEntry.clabWIFISSIDName
 * clabWIFISSIDName is subid 5 of clabWIFISSIDEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4.1.5
 * Description:
This object is defined in TR-181 Device.WiFi.SSID{i}.Name.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255t
 *
 * Ranges:  0 - 64;
 *
 * Its syntax is SnmpAdminString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 64)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param clabWIFISSIDName_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDName_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDName_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * clabWIFISSIDTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.clabWIFISSIDName).
 *    The length is in (one of) the range set(s):  0 - 64
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
clabWIFISSIDName_check_value( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDName_val_ptr,  size_t clabWIFISSIDName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDName_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDName_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid clabWIFISSIDName value.
     */

    return MFD_SUCCESS; /* clabWIFISSIDName value not illegal */
} /* clabWIFISSIDName_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * clabWIFISSIDTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
clabWIFISSIDName_undo_setup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDName_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup clabWIFISSIDName undo.
     */
    /*
     * copy clabWIFISSIDName and clabWIFISSIDName_len data
     * set rowreq_ctx->undo->clabWIFISSIDName from rowreq_ctx->data.clabWIFISSIDName
     */
    memcpy( rowreq_ctx->undo->clabWIFISSIDName, rowreq_ctx->data.clabWIFISSIDName,
            (rowreq_ctx->data.clabWIFISSIDName_len * sizeof(rowreq_ctx->undo->clabWIFISSIDName[0])));
    rowreq_ctx->undo->clabWIFISSIDName_len = rowreq_ctx->data.clabWIFISSIDName_len;


    return MFD_SUCCESS;
} /* clabWIFISSIDName_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param clabWIFISSIDName_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDName_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDName_val_ptr
 */
int
clabWIFISSIDName_set( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDName_val_ptr,  size_t clabWIFISSIDName_val_ptr_len )
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDName_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDName_val_ptr);

    /*
     * TODO:461:M: |-> Set clabWIFISSIDName value.
     * set clabWIFISSIDName value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDName, clabWIFISSIDName_val_ptr, clabWIFISSIDName_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.clabWIFISSIDName_len = clabWIFISSIDName_val_ptr_len / sizeof(clabWIFISSIDName_val_ptr[0]);

    return MFD_SUCCESS;
} /* clabWIFISSIDName_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
clabWIFISSIDName_undo( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDName_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up clabWIFISSIDName undo.
     */
    /*
     * copy clabWIFISSIDName and clabWIFISSIDName_len data
     * set rowreq_ctx->data.clabWIFISSIDName from rowreq_ctx->undo->clabWIFISSIDName
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDName, rowreq_ctx->undo->clabWIFISSIDName,
            (rowreq_ctx->undo->clabWIFISSIDName_len * sizeof(rowreq_ctx->data.clabWIFISSIDName[0])));
    rowreq_ctx->data.clabWIFISSIDName_len = rowreq_ctx->undo->clabWIFISSIDName_len;

    
    return MFD_SUCCESS;
} /* clabWIFISSIDName_undo */

/*---------------------------------------------------------------------
 * CLAB-WIFI-MIB::clabWIFISSIDEntry.clabWIFISSIDLastChange
 * clabWIFISSIDLastChange is subid 6 of clabWIFISSIDEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4.1.6
 * Description:
This object is defined in TR-181 Device.WiFi.SSID{i}.LastChange.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param clabWIFISSIDLastChange_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * clabWIFISSIDTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
clabWIFISSIDLastChange_check_value( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, u_long clabWIFISSIDLastChange_val)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDLastChange_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid clabWIFISSIDLastChange value.
     */

    return MFD_SUCCESS; /* clabWIFISSIDLastChange value not illegal */
} /* clabWIFISSIDLastChange_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * clabWIFISSIDTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
clabWIFISSIDLastChange_undo_setup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDLastChange_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup clabWIFISSIDLastChange undo.
     */
    /*
     * copy clabWIFISSIDLastChange data
     * set rowreq_ctx->undo->clabWIFISSIDLastChange from rowreq_ctx->data.clabWIFISSIDLastChange
     */
    rowreq_ctx->undo->clabWIFISSIDLastChange = rowreq_ctx->data.clabWIFISSIDLastChange;


    return MFD_SUCCESS;
} /* clabWIFISSIDLastChange_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param clabWIFISSIDLastChange_val
 *        A u_long containing the new value.
 */
int
clabWIFISSIDLastChange_set( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, u_long clabWIFISSIDLastChange_val )
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDLastChange_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set clabWIFISSIDLastChange value.
     * set clabWIFISSIDLastChange value in rowreq_ctx->data
     */
    rowreq_ctx->data.clabWIFISSIDLastChange = clabWIFISSIDLastChange_val;

    return MFD_SUCCESS;
} /* clabWIFISSIDLastChange_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
clabWIFISSIDLastChange_undo( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDLastChange_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up clabWIFISSIDLastChange undo.
     */
    /*
     * copy clabWIFISSIDLastChange data
     * set rowreq_ctx->data.clabWIFISSIDLastChange from rowreq_ctx->undo->clabWIFISSIDLastChange
     */
    rowreq_ctx->data.clabWIFISSIDLastChange = rowreq_ctx->undo->clabWIFISSIDLastChange;

    
    return MFD_SUCCESS;
} /* clabWIFISSIDLastChange_undo */

/*---------------------------------------------------------------------
 * CLAB-WIFI-MIB::clabWIFISSIDEntry.clabWIFISSIDLowerLayers
 * clabWIFISSIDLowerLayers is subid 7 of clabWIFISSIDEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4.1.7
 * Description:
This object is defined in TR-181 Device.WiFi.SSID{i}.LowerLayers.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255t
 *
 * Ranges:  0 - 1024;
 *
 * Its syntax is SnmpAdminString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 1024)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param clabWIFISSIDLowerLayers_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDLowerLayers_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDLowerLayers_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * clabWIFISSIDTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.clabWIFISSIDLowerLayers).
 *    The length is in (one of) the range set(s):  0 - 1024
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
clabWIFISSIDLowerLayers_check_value( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDLowerLayers_val_ptr,  size_t clabWIFISSIDLowerLayers_val_ptr_len)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDLowerLayers_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDLowerLayers_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid clabWIFISSIDLowerLayers value.
     */

    return MFD_SUCCESS; /* clabWIFISSIDLowerLayers value not illegal */
} /* clabWIFISSIDLowerLayers_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * clabWIFISSIDTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
clabWIFISSIDLowerLayers_undo_setup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDLowerLayers_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup clabWIFISSIDLowerLayers undo.
     */
    /*
     * copy clabWIFISSIDLowerLayers and clabWIFISSIDLowerLayers_len data
     * set rowreq_ctx->undo->clabWIFISSIDLowerLayers from rowreq_ctx->data.clabWIFISSIDLowerLayers
     */
    memcpy( rowreq_ctx->undo->clabWIFISSIDLowerLayers, rowreq_ctx->data.clabWIFISSIDLowerLayers,
            (rowreq_ctx->data.clabWIFISSIDLowerLayers_len * sizeof(rowreq_ctx->undo->clabWIFISSIDLowerLayers[0])));
    rowreq_ctx->undo->clabWIFISSIDLowerLayers_len = rowreq_ctx->data.clabWIFISSIDLowerLayers_len;


    return MFD_SUCCESS;
} /* clabWIFISSIDLowerLayers_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param clabWIFISSIDLowerLayers_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDLowerLayers_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDLowerLayers_val_ptr
 */
int
clabWIFISSIDLowerLayers_set( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDLowerLayers_val_ptr,  size_t clabWIFISSIDLowerLayers_val_ptr_len )
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDLowerLayers_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDLowerLayers_val_ptr);

    /*
     * TODO:461:M: |-> Set clabWIFISSIDLowerLayers value.
     * set clabWIFISSIDLowerLayers value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDLowerLayers, clabWIFISSIDLowerLayers_val_ptr, clabWIFISSIDLowerLayers_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.clabWIFISSIDLowerLayers_len = clabWIFISSIDLowerLayers_val_ptr_len / sizeof(clabWIFISSIDLowerLayers_val_ptr[0]);

    return MFD_SUCCESS;
} /* clabWIFISSIDLowerLayers_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
clabWIFISSIDLowerLayers_undo( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDLowerLayers_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up clabWIFISSIDLowerLayers undo.
     */
    /*
     * copy clabWIFISSIDLowerLayers and clabWIFISSIDLowerLayers_len data
     * set rowreq_ctx->data.clabWIFISSIDLowerLayers from rowreq_ctx->undo->clabWIFISSIDLowerLayers
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDLowerLayers, rowreq_ctx->undo->clabWIFISSIDLowerLayers,
            (rowreq_ctx->undo->clabWIFISSIDLowerLayers_len * sizeof(rowreq_ctx->data.clabWIFISSIDLowerLayers[0])));
    rowreq_ctx->data.clabWIFISSIDLowerLayers_len = rowreq_ctx->undo->clabWIFISSIDLowerLayers_len;

    
    return MFD_SUCCESS;
} /* clabWIFISSIDLowerLayers_undo */

/*---------------------------------------------------------------------
 * CLAB-WIFI-MIB::clabWIFISSIDEntry.clabWIFISSIDBSSID
 * clabWIFISSIDBSSID is subid 8 of clabWIFISSIDEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4.1.8
 * Description:
This object is defined in TR-181 Device.WiFi.SSID{i}.BSSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 1x:
 *
 * Ranges:  6;
 *
 * Its syntax is MacAddress (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 6)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param clabWIFISSIDBSSID_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDBSSID_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDBSSID_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * clabWIFISSIDTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.clabWIFISSIDBSSID).
 *    The length is in (one of) the range set(s):  6
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
clabWIFISSIDBSSID_check_value( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDBSSID_val_ptr,  size_t clabWIFISSIDBSSID_val_ptr_len)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDBSSID_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDBSSID_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid clabWIFISSIDBSSID value.
     */

    return MFD_SUCCESS; /* clabWIFISSIDBSSID value not illegal */
} /* clabWIFISSIDBSSID_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * clabWIFISSIDTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
clabWIFISSIDBSSID_undo_setup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDBSSID_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup clabWIFISSIDBSSID undo.
     */
    /*
     * copy clabWIFISSIDBSSID and clabWIFISSIDBSSID_len data
     * set rowreq_ctx->undo->clabWIFISSIDBSSID from rowreq_ctx->data.clabWIFISSIDBSSID
     */
    memcpy( rowreq_ctx->undo->clabWIFISSIDBSSID, rowreq_ctx->data.clabWIFISSIDBSSID,
            (rowreq_ctx->data.clabWIFISSIDBSSID_len * sizeof(rowreq_ctx->undo->clabWIFISSIDBSSID[0])));
    rowreq_ctx->undo->clabWIFISSIDBSSID_len = rowreq_ctx->data.clabWIFISSIDBSSID_len;


    return MFD_SUCCESS;
} /* clabWIFISSIDBSSID_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param clabWIFISSIDBSSID_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDBSSID_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDBSSID_val_ptr
 */
int
clabWIFISSIDBSSID_set( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDBSSID_val_ptr,  size_t clabWIFISSIDBSSID_val_ptr_len )
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDBSSID_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDBSSID_val_ptr);

    /*
     * TODO:461:M: |-> Set clabWIFISSIDBSSID value.
     * set clabWIFISSIDBSSID value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDBSSID, clabWIFISSIDBSSID_val_ptr, clabWIFISSIDBSSID_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.clabWIFISSIDBSSID_len = clabWIFISSIDBSSID_val_ptr_len / sizeof(clabWIFISSIDBSSID_val_ptr[0]);

    return MFD_SUCCESS;
} /* clabWIFISSIDBSSID_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
clabWIFISSIDBSSID_undo( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDBSSID_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up clabWIFISSIDBSSID undo.
     */
    /*
     * copy clabWIFISSIDBSSID and clabWIFISSIDBSSID_len data
     * set rowreq_ctx->data.clabWIFISSIDBSSID from rowreq_ctx->undo->clabWIFISSIDBSSID
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDBSSID, rowreq_ctx->undo->clabWIFISSIDBSSID,
            (rowreq_ctx->undo->clabWIFISSIDBSSID_len * sizeof(rowreq_ctx->data.clabWIFISSIDBSSID[0])));
    rowreq_ctx->data.clabWIFISSIDBSSID_len = rowreq_ctx->undo->clabWIFISSIDBSSID_len;

    
    return MFD_SUCCESS;
} /* clabWIFISSIDBSSID_undo */

/*---------------------------------------------------------------------
 * CLAB-WIFI-MIB::clabWIFISSIDEntry.clabWIFISSIDMACAddress
 * clabWIFISSIDMACAddress is subid 9 of clabWIFISSIDEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4.1.9
 * Description:
This object is defined in TR-181 Device.WiFi.SSID{i}.MACAddress.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 1x:
 *
 * Ranges:  6;
 *
 * Its syntax is MacAddress (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 6)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param clabWIFISSIDMACAddress_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDMACAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDMACAddress_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * clabWIFISSIDTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.clabWIFISSIDMACAddress).
 *    The length is in (one of) the range set(s):  6
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
clabWIFISSIDMACAddress_check_value( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDMACAddress_val_ptr,  size_t clabWIFISSIDMACAddress_val_ptr_len)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDMACAddress_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDMACAddress_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid clabWIFISSIDMACAddress value.
     */

    return MFD_SUCCESS; /* clabWIFISSIDMACAddress value not illegal */
} /* clabWIFISSIDMACAddress_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * clabWIFISSIDTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
clabWIFISSIDMACAddress_undo_setup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDMACAddress_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup clabWIFISSIDMACAddress undo.
     */
    /*
     * copy clabWIFISSIDMACAddress and clabWIFISSIDMACAddress_len data
     * set rowreq_ctx->undo->clabWIFISSIDMACAddress from rowreq_ctx->data.clabWIFISSIDMACAddress
     */
    memcpy( rowreq_ctx->undo->clabWIFISSIDMACAddress, rowreq_ctx->data.clabWIFISSIDMACAddress,
            (rowreq_ctx->data.clabWIFISSIDMACAddress_len * sizeof(rowreq_ctx->undo->clabWIFISSIDMACAddress[0])));
    rowreq_ctx->undo->clabWIFISSIDMACAddress_len = rowreq_ctx->data.clabWIFISSIDMACAddress_len;


    return MFD_SUCCESS;
} /* clabWIFISSIDMACAddress_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param clabWIFISSIDMACAddress_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDMACAddress_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDMACAddress_val_ptr
 */
int
clabWIFISSIDMACAddress_set( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDMACAddress_val_ptr,  size_t clabWIFISSIDMACAddress_val_ptr_len )
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDMACAddress_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDMACAddress_val_ptr);

    /*
     * TODO:461:M: |-> Set clabWIFISSIDMACAddress value.
     * set clabWIFISSIDMACAddress value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDMACAddress, clabWIFISSIDMACAddress_val_ptr, clabWIFISSIDMACAddress_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.clabWIFISSIDMACAddress_len = clabWIFISSIDMACAddress_val_ptr_len / sizeof(clabWIFISSIDMACAddress_val_ptr[0]);

    return MFD_SUCCESS;
} /* clabWIFISSIDMACAddress_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
clabWIFISSIDMACAddress_undo( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDMACAddress_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up clabWIFISSIDMACAddress undo.
     */
    /*
     * copy clabWIFISSIDMACAddress and clabWIFISSIDMACAddress_len data
     * set rowreq_ctx->data.clabWIFISSIDMACAddress from rowreq_ctx->undo->clabWIFISSIDMACAddress
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDMACAddress, rowreq_ctx->undo->clabWIFISSIDMACAddress,
            (rowreq_ctx->undo->clabWIFISSIDMACAddress_len * sizeof(rowreq_ctx->data.clabWIFISSIDMACAddress[0])));
    rowreq_ctx->data.clabWIFISSIDMACAddress_len = rowreq_ctx->undo->clabWIFISSIDMACAddress_len;

    
    return MFD_SUCCESS;
} /* clabWIFISSIDMACAddress_undo */

/*---------------------------------------------------------------------
 * CLAB-WIFI-MIB::clabWIFISSIDEntry.clabWIFISSIDSSID
 * clabWIFISSIDSSID is subid 10 of clabWIFISSIDEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4.1.10
 * Description:
This object is defined in TR-181 Device.WiFi.SSID{i}.SSID.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255t
 *
 * Ranges:  0 - 32;
 *
 * Its syntax is SnmpAdminString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 32)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param clabWIFISSIDSSID_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDSSID_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDSSID_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * clabWIFISSIDTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.clabWIFISSIDSSID).
 *    The length is in (one of) the range set(s):  0 - 32
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
clabWIFISSIDSSID_check_value( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDSSID_val_ptr,  size_t clabWIFISSIDSSID_val_ptr_len)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDSSID_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDSSID_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid clabWIFISSIDSSID value.
     */

    return MFD_SUCCESS; /* clabWIFISSIDSSID value not illegal */
} /* clabWIFISSIDSSID_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * clabWIFISSIDTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
clabWIFISSIDSSID_undo_setup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDSSID_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup clabWIFISSIDSSID undo.
     */
    /*
     * copy clabWIFISSIDSSID and clabWIFISSIDSSID_len data
     * set rowreq_ctx->undo->clabWIFISSIDSSID from rowreq_ctx->data.clabWIFISSIDSSID
     */
    memcpy( rowreq_ctx->undo->clabWIFISSIDSSID, rowreq_ctx->data.clabWIFISSIDSSID,
            (rowreq_ctx->data.clabWIFISSIDSSID_len * sizeof(rowreq_ctx->undo->clabWIFISSIDSSID[0])));
    rowreq_ctx->undo->clabWIFISSIDSSID_len = rowreq_ctx->data.clabWIFISSIDSSID_len;


    return MFD_SUCCESS;
} /* clabWIFISSIDSSID_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param clabWIFISSIDSSID_val_ptr
 *        A char containing the new value.
 * @param clabWIFISSIDSSID_val_ptr_len
 *        The size (in bytes) of the data pointed to by clabWIFISSIDSSID_val_ptr
 */
int
clabWIFISSIDSSID_set( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, char *clabWIFISSIDSSID_val_ptr,  size_t clabWIFISSIDSSID_val_ptr_len )
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDSSID_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != clabWIFISSIDSSID_val_ptr);

    /*
     * TODO:461:M: |-> Set clabWIFISSIDSSID value.
     * set clabWIFISSIDSSID value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDSSID, clabWIFISSIDSSID_val_ptr, clabWIFISSIDSSID_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.clabWIFISSIDSSID_len = clabWIFISSIDSSID_val_ptr_len / sizeof(clabWIFISSIDSSID_val_ptr[0]);

    return MFD_SUCCESS;
} /* clabWIFISSIDSSID_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
clabWIFISSIDSSID_undo( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDSSID_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up clabWIFISSIDSSID undo.
     */
    /*
     * copy clabWIFISSIDSSID and clabWIFISSIDSSID_len data
     * set rowreq_ctx->data.clabWIFISSIDSSID from rowreq_ctx->undo->clabWIFISSIDSSID
     */
    memcpy( rowreq_ctx->data.clabWIFISSIDSSID, rowreq_ctx->undo->clabWIFISSIDSSID,
            (rowreq_ctx->undo->clabWIFISSIDSSID_len * sizeof(rowreq_ctx->data.clabWIFISSIDSSID[0])));
    rowreq_ctx->data.clabWIFISSIDSSID_len = rowreq_ctx->undo->clabWIFISSIDSSID_len;

    
    return MFD_SUCCESS;
} /* clabWIFISSIDSSID_undo */

/*---------------------------------------------------------------------
 * CLAB-WIFI-MIB::clabWIFISSIDEntry.clabWIFISSIDRowStatus
 * clabWIFISSIDRowStatus is subid 11 of clabWIFISSIDEntry.
 * Its status is Current, and its access level is Create.
 * OID: .1.3.6.1.4.1.4491.2.5.1.1.4.1.11
 * Description:
The status of this instance
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * Its syntax is RowStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param clabWIFISSIDRowStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * clabWIFISSIDTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  active(1), notInService(2), notReady(3), createAndGo(4), createAndWait(5), destroy(6)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
clabWIFISSIDRowStatus_check_value( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, u_long clabWIFISSIDRowStatus_val)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDRowStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid clabWIFISSIDRowStatus value.
     */

    return MFD_SUCCESS; /* clabWIFISSIDRowStatus value not illegal */
} /* clabWIFISSIDRowStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (clabWIFISSIDTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * clabWIFISSIDTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
clabWIFISSIDRowStatus_undo_setup( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDRowStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup clabWIFISSIDRowStatus undo.
     */
    /*
     * copy clabWIFISSIDRowStatus data
     * set rowreq_ctx->undo->clabWIFISSIDRowStatus from rowreq_ctx->data.clabWIFISSIDRowStatus
     */
    rowreq_ctx->undo->clabWIFISSIDRowStatus = rowreq_ctx->data.clabWIFISSIDRowStatus;


    return MFD_SUCCESS;
} /* clabWIFISSIDRowStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param clabWIFISSIDRowStatus_val
 *        A long containing the new value.
 */
int
clabWIFISSIDRowStatus_set( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx, u_long clabWIFISSIDRowStatus_val )
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDRowStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set clabWIFISSIDRowStatus value.
     * set clabWIFISSIDRowStatus value in rowreq_ctx->data
     */
    rowreq_ctx->data.clabWIFISSIDRowStatus = clabWIFISSIDRowStatus_val;

    return MFD_SUCCESS;
} /* clabWIFISSIDRowStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
clabWIFISSIDRowStatus_undo( clabWIFISSIDTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:clabWIFISSIDTable:clabWIFISSIDRowStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up clabWIFISSIDRowStatus undo.
     */
    /*
     * copy clabWIFISSIDRowStatus data
     * set rowreq_ctx->data.clabWIFISSIDRowStatus from rowreq_ctx->undo->clabWIFISSIDRowStatus
     */
    rowreq_ctx->data.clabWIFISSIDRowStatus = rowreq_ctx->undo->clabWIFISSIDRowStatus;

    
    return MFD_SUCCESS;
} /* clabWIFISSIDRowStatus_undo */

/** @} */
