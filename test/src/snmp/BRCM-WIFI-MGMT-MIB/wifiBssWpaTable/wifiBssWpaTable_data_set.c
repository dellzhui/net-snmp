/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "wifiBssWpaTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table wifiBssWpaTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * BRCM-WIFI-MGMT-MIB::wifiBssWpaTable is subid 4 of wifiMbssSecurity.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.4413.2.2.2.1.18.1.2.3.4, length: 16
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * wifiBssWpaTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * wifiBssWpaTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (wifiBssWpaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
wifiBssWpaTable_undo_setup( wifiBssWpaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup wifiBssWpaTable undo.
     * set up wifiBssWpaTable undo information, in preparation for a set.
     * Undo storage is in (* wifiBssWpaGroupRekeyInterval_val_ptr )*
     */

    return rc;
} /* wifiBssWpaTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (wifiBssWpaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
wifiBssWpaTable_undo( wifiBssWpaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> wifiBssWpaTable undo.
     * wifiBssWpaTable undo information, in response to a failed set.
     * Undo storage is in (* wifiBssWpaGroupRekeyInterval_val_ptr )*
     */

    return rc;
} /* wifiBssWpaTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (wifiBssWpaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
wifiBssWpaTable_undo_cleanup( wifiBssWpaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup wifiBssWpaTable undo.
     * Undo storage is in (* wifiBssWpaGroupRekeyInterval_val_ptr )*
     */

    return rc;
} /* wifiBssWpaTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * wifiBssWpaTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param wifiBssWpaTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
wifiBssWpaTable_commit( wifiBssWpaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit wifiBssWpaTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_WIFIBSSWPAALGORITHM_FLAG) {
       save_flags &= ~COLUMN_WIFIBSSWPAALGORITHM_FLAG; /* clear wifiBssWpaAlgorithm */
       /*
        * TODO:482:o: |-> commit column wifiBssWpaAlgorithm.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"wifiBssWpaTable column wifiBssWpaAlgorithm commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wifiBssWpaAlgorithm
             */
            rowreq_ctx->column_set_flags |= COLUMN_WIFIBSSWPAALGORITHM_FLAG;
       }
    }

    if (save_flags & COLUMN_WIFIBSSWPAPRESHAREDKEY_FLAG) {
       save_flags &= ~COLUMN_WIFIBSSWPAPRESHAREDKEY_FLAG; /* clear wifiBssWpaPreSharedKey */
       /*
        * TODO:482:o: |-> commit column wifiBssWpaPreSharedKey.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"wifiBssWpaTable column wifiBssWpaPreSharedKey commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wifiBssWpaPreSharedKey
             */
            rowreq_ctx->column_set_flags |= COLUMN_WIFIBSSWPAPRESHAREDKEY_FLAG;
       }
    }

    if (save_flags & COLUMN_WIFIBSSWPAGROUPREKEYINTERVAL_FLAG) {
       save_flags &= ~COLUMN_WIFIBSSWPAGROUPREKEYINTERVAL_FLAG; /* clear wifiBssWpaGroupRekeyInterval */
       /*
        * TODO:482:o: |-> commit column wifiBssWpaGroupRekeyInterval.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"wifiBssWpaTable column wifiBssWpaGroupRekeyInterval commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo wifiBssWpaGroupRekeyInterval
             */
            rowreq_ctx->column_set_flags |= COLUMN_WIFIBSSWPAGROUPREKEYINTERVAL_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* wifiBssWpaTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * wifiBssWpaTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param wifiBssWpaTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
wifiBssWpaTable_undo_commit( wifiBssWpaTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo wifiBssWpaTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* wifiBssWpaTable_undo_commit */

/*
 * TODO:440:M: Implement wifiBssWpaTable node value checks.
 * TODO:450:M: Implement wifiBssWpaTable undo functions.
 * TODO:460:M: Implement wifiBssWpaTable set functions.
 * TODO:480:M: Implement wifiBssWpaTable commit functions.
 */
/*---------------------------------------------------------------------
 * BRCM-WIFI-MGMT-MIB::wifiBssWpaEntry.wifiBssWpaAlgorithm
 * wifiBssWpaAlgorithm is subid 1 of wifiBssWpaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4413.2.2.2.1.18.1.2.3.4.1.1
 * Description:
Controls and reflects the WPA encryption mode used by the service set.  
         The WPA algorithm for WPA2-PSK and WPA2-Enterprise security mode is 
         either aes(2) or tkipPlusAes(3).  If feature is disabled, then none (1)
         is returned.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  none(1), aes(2), tkipPlusAes(3)
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wifiBssWpaAlgorithm_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * wifiBssWpaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  none(1), aes(2), tkipPlusAes(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wifiBssWpaAlgorithm_check_value( wifiBssWpaTable_rowreq_ctx *rowreq_ctx, u_long wifiBssWpaAlgorithm_val)
{
    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaAlgorithm_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wifiBssWpaAlgorithm value.
     */

    return MFD_SUCCESS; /* wifiBssWpaAlgorithm value not illegal */
} /* wifiBssWpaAlgorithm_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (wifiBssWpaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * wifiBssWpaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wifiBssWpaAlgorithm_undo_setup( wifiBssWpaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaAlgorithm_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wifiBssWpaAlgorithm undo.
     */
    /*
     * copy wifiBssWpaAlgorithm data
     * set rowreq_ctx->undo->wifiBssWpaAlgorithm from rowreq_ctx->data.wifiBssWpaAlgorithm
     */
    rowreq_ctx->undo->wifiBssWpaAlgorithm = rowreq_ctx->data.wifiBssWpaAlgorithm;


    return MFD_SUCCESS;
} /* wifiBssWpaAlgorithm_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wifiBssWpaAlgorithm_val
 *        A long containing the new value.
 */
int
wifiBssWpaAlgorithm_set( wifiBssWpaTable_rowreq_ctx *rowreq_ctx, u_long wifiBssWpaAlgorithm_val )
{

    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaAlgorithm_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wifiBssWpaAlgorithm value.
     * set wifiBssWpaAlgorithm value in rowreq_ctx->data
     */
    rowreq_ctx->data.wifiBssWpaAlgorithm = wifiBssWpaAlgorithm_val;

    return MFD_SUCCESS;
} /* wifiBssWpaAlgorithm_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wifiBssWpaAlgorithm_undo( wifiBssWpaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaAlgorithm_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wifiBssWpaAlgorithm undo.
     */
    /*
     * copy wifiBssWpaAlgorithm data
     * set rowreq_ctx->data.wifiBssWpaAlgorithm from rowreq_ctx->undo->wifiBssWpaAlgorithm
     */
    rowreq_ctx->data.wifiBssWpaAlgorithm = rowreq_ctx->undo->wifiBssWpaAlgorithm;

    
    return MFD_SUCCESS;
} /* wifiBssWpaAlgorithm_undo */

/*---------------------------------------------------------------------
 * BRCM-WIFI-MGMT-MIB::wifiBssWpaEntry.wifiBssWpaPreSharedKey
 * wifiBssWpaPreSharedKey is subid 2 of wifiBssWpaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4413.2.2.2.1.18.1.2.3.4.1.2
 * Description:
Sets the WPA Pre-Shared Key (PSK) used by this service set.  This 
         value MUST be either a 64 byte hexadecimal number, OR an 8 to 63 
         character ASCII string.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  8 - 64;
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 64)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wifiBssWpaPreSharedKey_val_ptr
 *        A char containing the new value.
 * @param wifiBssWpaPreSharedKey_val_ptr_len
 *        The size (in bytes) of the data pointed to by wifiBssWpaPreSharedKey_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * wifiBssWpaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.wifiBssWpaPreSharedKey).
 *    The length is in (one of) the range set(s):  8 - 64
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wifiBssWpaPreSharedKey_check_value( wifiBssWpaTable_rowreq_ctx *rowreq_ctx, char *wifiBssWpaPreSharedKey_val_ptr,  size_t wifiBssWpaPreSharedKey_val_ptr_len)
{
    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaPreSharedKey_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wifiBssWpaPreSharedKey_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid wifiBssWpaPreSharedKey value.
     */

    return MFD_SUCCESS; /* wifiBssWpaPreSharedKey value not illegal */
} /* wifiBssWpaPreSharedKey_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (wifiBssWpaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * wifiBssWpaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wifiBssWpaPreSharedKey_undo_setup( wifiBssWpaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaPreSharedKey_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wifiBssWpaPreSharedKey undo.
     */
    /*
     * copy wifiBssWpaPreSharedKey and wifiBssWpaPreSharedKey_len data
     * set rowreq_ctx->undo->wifiBssWpaPreSharedKey from rowreq_ctx->data.wifiBssWpaPreSharedKey
     */
    memcpy( rowreq_ctx->undo->wifiBssWpaPreSharedKey, rowreq_ctx->data.wifiBssWpaPreSharedKey,
            (rowreq_ctx->data.wifiBssWpaPreSharedKey_len * sizeof(rowreq_ctx->undo->wifiBssWpaPreSharedKey[0])));
    rowreq_ctx->undo->wifiBssWpaPreSharedKey_len = rowreq_ctx->data.wifiBssWpaPreSharedKey_len;


    return MFD_SUCCESS;
} /* wifiBssWpaPreSharedKey_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wifiBssWpaPreSharedKey_val_ptr
 *        A char containing the new value.
 * @param wifiBssWpaPreSharedKey_val_ptr_len
 *        The size (in bytes) of the data pointed to by wifiBssWpaPreSharedKey_val_ptr
 */
int
wifiBssWpaPreSharedKey_set( wifiBssWpaTable_rowreq_ctx *rowreq_ctx, char *wifiBssWpaPreSharedKey_val_ptr,  size_t wifiBssWpaPreSharedKey_val_ptr_len )
{

    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaPreSharedKey_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != wifiBssWpaPreSharedKey_val_ptr);

    /*
     * TODO:461:M: |-> Set wifiBssWpaPreSharedKey value.
     * set wifiBssWpaPreSharedKey value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.wifiBssWpaPreSharedKey, wifiBssWpaPreSharedKey_val_ptr, wifiBssWpaPreSharedKey_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.wifiBssWpaPreSharedKey_len = wifiBssWpaPreSharedKey_val_ptr_len / sizeof(wifiBssWpaPreSharedKey_val_ptr[0]);

    return MFD_SUCCESS;
} /* wifiBssWpaPreSharedKey_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wifiBssWpaPreSharedKey_undo( wifiBssWpaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaPreSharedKey_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wifiBssWpaPreSharedKey undo.
     */
    /*
     * copy wifiBssWpaPreSharedKey and wifiBssWpaPreSharedKey_len data
     * set rowreq_ctx->data.wifiBssWpaPreSharedKey from rowreq_ctx->undo->wifiBssWpaPreSharedKey
     */
    memcpy( rowreq_ctx->data.wifiBssWpaPreSharedKey, rowreq_ctx->undo->wifiBssWpaPreSharedKey,
            (rowreq_ctx->undo->wifiBssWpaPreSharedKey_len * sizeof(rowreq_ctx->data.wifiBssWpaPreSharedKey[0])));
    rowreq_ctx->data.wifiBssWpaPreSharedKey_len = rowreq_ctx->undo->wifiBssWpaPreSharedKey_len;

    
    return MFD_SUCCESS;
} /* wifiBssWpaPreSharedKey_undo */

/*---------------------------------------------------------------------
 * BRCM-WIFI-MGMT-MIB::wifiBssWpaEntry.wifiBssWpaGroupRekeyInterval
 * wifiBssWpaGroupRekeyInterval is subid 3 of wifiBssWpaEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4413.2.2.2.1.18.1.2.3.4.1.3
 * Description:
Sets the WPA Group Rekey Interval for this service set. If set to zero, 
         periodic rekeying is disabled for this service set.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is UNSIGNED32 (based on perltype UNSIGNED32)
 * The net-snmp type is ASN_UNSIGNED. The C type decl is u_long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param wifiBssWpaGroupRekeyInterval_val
 *        A u_long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * wifiBssWpaTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_UNSIGNED
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
wifiBssWpaGroupRekeyInterval_check_value( wifiBssWpaTable_rowreq_ctx *rowreq_ctx, u_long wifiBssWpaGroupRekeyInterval_val)
{
    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaGroupRekeyInterval_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid wifiBssWpaGroupRekeyInterval value.
     */

    return MFD_SUCCESS; /* wifiBssWpaGroupRekeyInterval value not illegal */
} /* wifiBssWpaGroupRekeyInterval_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (wifiBssWpaTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * wifiBssWpaTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
wifiBssWpaGroupRekeyInterval_undo_setup( wifiBssWpaTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaGroupRekeyInterval_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup wifiBssWpaGroupRekeyInterval undo.
     */
    /*
     * copy wifiBssWpaGroupRekeyInterval data
     * set rowreq_ctx->undo->wifiBssWpaGroupRekeyInterval from rowreq_ctx->data.wifiBssWpaGroupRekeyInterval
     */
    rowreq_ctx->undo->wifiBssWpaGroupRekeyInterval = rowreq_ctx->data.wifiBssWpaGroupRekeyInterval;


    return MFD_SUCCESS;
} /* wifiBssWpaGroupRekeyInterval_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param wifiBssWpaGroupRekeyInterval_val
 *        A u_long containing the new value.
 */
int
wifiBssWpaGroupRekeyInterval_set( wifiBssWpaTable_rowreq_ctx *rowreq_ctx, u_long wifiBssWpaGroupRekeyInterval_val )
{

    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaGroupRekeyInterval_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set wifiBssWpaGroupRekeyInterval value.
     * set wifiBssWpaGroupRekeyInterval value in rowreq_ctx->data
     */
    rowreq_ctx->data.wifiBssWpaGroupRekeyInterval = wifiBssWpaGroupRekeyInterval_val;

    return MFD_SUCCESS;
} /* wifiBssWpaGroupRekeyInterval_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
wifiBssWpaGroupRekeyInterval_undo( wifiBssWpaTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:wifiBssWpaTable:wifiBssWpaGroupRekeyInterval_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up wifiBssWpaGroupRekeyInterval undo.
     */
    /*
     * copy wifiBssWpaGroupRekeyInterval data
     * set rowreq_ctx->data.wifiBssWpaGroupRekeyInterval from rowreq_ctx->undo->wifiBssWpaGroupRekeyInterval
     */
    rowreq_ctx->data.wifiBssWpaGroupRekeyInterval = rowreq_ctx->undo->wifiBssWpaGroupRekeyInterval;

    
    return MFD_SUCCESS;
} /* wifiBssWpaGroupRekeyInterval_undo */

/** @} */
