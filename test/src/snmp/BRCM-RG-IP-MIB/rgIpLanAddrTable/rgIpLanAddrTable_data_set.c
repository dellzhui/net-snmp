/*
 * Note: this file originally auto-generated by mib2c using
 *       version $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "rgIpLanAddrTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table rgIpLanAddrTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * BRCM-RG-IP-MIB::rgIpLanAddrTable is subid 1 of rgIpLanAddr.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.4413.2.2.2.1.7.2.3.1, length: 15
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rgIpLanAddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rgIpLanAddrTable_undo_setup( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup rgIpLanAddrTable undo.
     * set up rgIpLanAddrTable undo information, in preparation for a set.
     * Undo storage is in (* rgIpLanAddrHostName_val_ptr_ptr )*
     */

    return rc;
} /* rgIpLanAddrTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rgIpLanAddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
rgIpLanAddrTable_undo( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> rgIpLanAddrTable undo.
     * rgIpLanAddrTable undo information, in response to a failed set.
     * Undo storage is in (* rgIpLanAddrHostName_val_ptr_ptr )*
     */

    return rc;
} /* rgIpLanAddrTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rgIpLanAddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rgIpLanAddrTable_undo_cleanup( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup rgIpLanAddrTable undo.
     * Undo storage is in (* rgIpLanAddrHostName_val_ptr_ptr )*
     */

    return rc;
} /* rgIpLanAddrTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rgIpLanAddrTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rgIpLanAddrTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rgIpLanAddrTable_commit( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit rgIpLanAddrTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_RGIPLANADDRCLIENTID_FLAG) {
       save_flags &= ~COLUMN_RGIPLANADDRCLIENTID_FLAG; /* clear rgIpLanAddrClientID */
       /*
        * TODO:482:o: |-> commit column rgIpLanAddrClientID.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"rgIpLanAddrTable column rgIpLanAddrClientID commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo rgIpLanAddrClientID
             */
            rowreq_ctx->column_set_flags |= COLUMN_RGIPLANADDRCLIENTID_FLAG;
       }
    }

    if (save_flags & COLUMN_RGIPLANADDRLEASECREATETIME_FLAG) {
       save_flags &= ~COLUMN_RGIPLANADDRLEASECREATETIME_FLAG; /* clear rgIpLanAddrLeaseCreateTime */
       /*
        * TODO:482:o: |-> commit column rgIpLanAddrLeaseCreateTime.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"rgIpLanAddrTable column rgIpLanAddrLeaseCreateTime commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo rgIpLanAddrLeaseCreateTime
             */
            rowreq_ctx->column_set_flags |= COLUMN_RGIPLANADDRLEASECREATETIME_FLAG;
       }
    }

    if (save_flags & COLUMN_RGIPLANADDRLEASEEXPIRETIME_FLAG) {
       save_flags &= ~COLUMN_RGIPLANADDRLEASEEXPIRETIME_FLAG; /* clear rgIpLanAddrLeaseExpireTime */
       /*
        * TODO:482:o: |-> commit column rgIpLanAddrLeaseExpireTime.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"rgIpLanAddrTable column rgIpLanAddrLeaseExpireTime commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo rgIpLanAddrLeaseExpireTime
             */
            rowreq_ctx->column_set_flags |= COLUMN_RGIPLANADDRLEASEEXPIRETIME_FLAG;
       }
    }

    if (save_flags & COLUMN_RGIPLANADDRHOSTNAME_FLAG) {
       save_flags &= ~COLUMN_RGIPLANADDRHOSTNAME_FLAG; /* clear rgIpLanAddrHostName */
       /*
        * TODO:482:o: |-> commit column rgIpLanAddrHostName.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"rgIpLanAddrTable column rgIpLanAddrHostName commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo rgIpLanAddrHostName
             */
            rowreq_ctx->column_set_flags |= COLUMN_RGIPLANADDRHOSTNAME_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* rgIpLanAddrTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * rgIpLanAddrTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param rgIpLanAddrTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
rgIpLanAddrTable_undo_commit( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo rgIpLanAddrTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* rgIpLanAddrTable_undo_commit */

/*
 * TODO:440:M: Implement rgIpLanAddrTable node value checks.
 * TODO:450:M: Implement rgIpLanAddrTable undo functions.
 * TODO:460:M: Implement rgIpLanAddrTable set functions.
 * TODO:480:M: Implement rgIpLanAddrTable commit functions.
 */
/*---------------------------------------------------------------------
 * BRCM-RG-IP-MIB::rgIpLanAddrEntry.rgIpLanAddrClientID
 * rgIpLanAddrClientID is subid 3 of rgIpLanAddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4413.2.2.2.1.7.2.3.1.1.3
 * Description:
The client's (i.e., LAN IP Device's) hardware address as
        indicated in the chaddr field of its DHCP REQUEST message.
        There is a one-to-one relationship between the hardware
        address and the LAN IP Device. 
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 80;
 *
 * Its syntax is OCTETSTR (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 80)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rgIpLanAddrClientID_val_ptr
 *        A char containing the new value.
 * @param rgIpLanAddrClientID_val_ptr_len
 *        The size (in bytes) of the data pointed to by rgIpLanAddrClientID_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rgIpLanAddrTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rgIpLanAddrClientID).
 *    The length is in (one of) the range set(s):  1 - 80
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rgIpLanAddrClientID_check_value( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx, char *rgIpLanAddrClientID_val_ptr,  size_t rgIpLanAddrClientID_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrClientID_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rgIpLanAddrClientID_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rgIpLanAddrClientID value.
     */

    return MFD_SUCCESS; /* rgIpLanAddrClientID value not illegal */
} /* rgIpLanAddrClientID_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rgIpLanAddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rgIpLanAddrTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rgIpLanAddrClientID_undo_setup( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrClientID_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rgIpLanAddrClientID undo.
     */
    /*
     * copy rgIpLanAddrClientID and rgIpLanAddrClientID_len data
     * set rowreq_ctx->undo->rgIpLanAddrClientID from rowreq_ctx->data.rgIpLanAddrClientID
     */
    memcpy( rowreq_ctx->undo->rgIpLanAddrClientID, rowreq_ctx->data.rgIpLanAddrClientID,
            (rowreq_ctx->data.rgIpLanAddrClientID_len * sizeof(rowreq_ctx->undo->rgIpLanAddrClientID[0])));
    rowreq_ctx->undo->rgIpLanAddrClientID_len = rowreq_ctx->data.rgIpLanAddrClientID_len;


    return MFD_SUCCESS;
} /* rgIpLanAddrClientID_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rgIpLanAddrClientID_val_ptr
 *        A char containing the new value.
 * @param rgIpLanAddrClientID_val_ptr_len
 *        The size (in bytes) of the data pointed to by rgIpLanAddrClientID_val_ptr
 */
int
rgIpLanAddrClientID_set( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx, char *rgIpLanAddrClientID_val_ptr,  size_t rgIpLanAddrClientID_val_ptr_len )
{

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrClientID_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rgIpLanAddrClientID_val_ptr);

    /*
     * TODO:461:M: |-> Set rgIpLanAddrClientID value.
     * set rgIpLanAddrClientID value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.rgIpLanAddrClientID, rgIpLanAddrClientID_val_ptr, rgIpLanAddrClientID_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.rgIpLanAddrClientID_len = rgIpLanAddrClientID_val_ptr_len / sizeof(rgIpLanAddrClientID_val_ptr[0]);

    return MFD_SUCCESS;
} /* rgIpLanAddrClientID_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rgIpLanAddrClientID_undo( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrClientID_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rgIpLanAddrClientID undo.
     */
    /*
     * copy rgIpLanAddrClientID and rgIpLanAddrClientID_len data
     * set rowreq_ctx->data.rgIpLanAddrClientID from rowreq_ctx->undo->rgIpLanAddrClientID
     */
    memcpy( rowreq_ctx->data.rgIpLanAddrClientID, rowreq_ctx->undo->rgIpLanAddrClientID,
            (rowreq_ctx->undo->rgIpLanAddrClientID_len * sizeof(rowreq_ctx->data.rgIpLanAddrClientID[0])));
    rowreq_ctx->data.rgIpLanAddrClientID_len = rowreq_ctx->undo->rgIpLanAddrClientID_len;

    
    return MFD_SUCCESS;
} /* rgIpLanAddrClientID_undo */

/*---------------------------------------------------------------------
 * BRCM-RG-IP-MIB::rgIpLanAddrEntry.rgIpLanAddrLeaseCreateTime
 * rgIpLanAddrLeaseCreateTime is subid 4 of rgIpLanAddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4413.2.2.2.1.7.2.3.1.1.4
 * Description:
This is the date and time when the LAN IP lease was
        created.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 2d-1d-1d,1d:1d:1d.1d,1a1d:1d
 *
 * Ranges:  8, 11;
 *
 * Its syntax is DateAndTime (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 11)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rgIpLanAddrLeaseCreateTime_val_ptr
 *        A char containing the new value.
 * @param rgIpLanAddrLeaseCreateTime_val_ptr_len
 *        The size (in bytes) of the data pointed to by rgIpLanAddrLeaseCreateTime_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rgIpLanAddrTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rgIpLanAddrLeaseCreateTime).
 *    The length is in (one of) the range set(s):  8, 11
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rgIpLanAddrLeaseCreateTime_check_value( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx, char *rgIpLanAddrLeaseCreateTime_val_ptr,  size_t rgIpLanAddrLeaseCreateTime_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrLeaseCreateTime_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rgIpLanAddrLeaseCreateTime_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rgIpLanAddrLeaseCreateTime value.
     */

    return MFD_SUCCESS; /* rgIpLanAddrLeaseCreateTime value not illegal */
} /* rgIpLanAddrLeaseCreateTime_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rgIpLanAddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rgIpLanAddrTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rgIpLanAddrLeaseCreateTime_undo_setup( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrLeaseCreateTime_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rgIpLanAddrLeaseCreateTime undo.
     */
    /*
     * copy rgIpLanAddrLeaseCreateTime and rgIpLanAddrLeaseCreateTime_len data
     * set rowreq_ctx->undo->rgIpLanAddrLeaseCreateTime from rowreq_ctx->data.rgIpLanAddrLeaseCreateTime
     */
    memcpy( rowreq_ctx->undo->rgIpLanAddrLeaseCreateTime, rowreq_ctx->data.rgIpLanAddrLeaseCreateTime,
            (rowreq_ctx->data.rgIpLanAddrLeaseCreateTime_len * sizeof(rowreq_ctx->undo->rgIpLanAddrLeaseCreateTime[0])));
    rowreq_ctx->undo->rgIpLanAddrLeaseCreateTime_len = rowreq_ctx->data.rgIpLanAddrLeaseCreateTime_len;


    return MFD_SUCCESS;
} /* rgIpLanAddrLeaseCreateTime_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rgIpLanAddrLeaseCreateTime_val_ptr
 *        A char containing the new value.
 * @param rgIpLanAddrLeaseCreateTime_val_ptr_len
 *        The size (in bytes) of the data pointed to by rgIpLanAddrLeaseCreateTime_val_ptr
 */
int
rgIpLanAddrLeaseCreateTime_set( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx, char *rgIpLanAddrLeaseCreateTime_val_ptr,  size_t rgIpLanAddrLeaseCreateTime_val_ptr_len )
{

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrLeaseCreateTime_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rgIpLanAddrLeaseCreateTime_val_ptr);

    /*
     * TODO:461:M: |-> Set rgIpLanAddrLeaseCreateTime value.
     * set rgIpLanAddrLeaseCreateTime value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.rgIpLanAddrLeaseCreateTime, rgIpLanAddrLeaseCreateTime_val_ptr, rgIpLanAddrLeaseCreateTime_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.rgIpLanAddrLeaseCreateTime_len = rgIpLanAddrLeaseCreateTime_val_ptr_len / sizeof(rgIpLanAddrLeaseCreateTime_val_ptr[0]);

    return MFD_SUCCESS;
} /* rgIpLanAddrLeaseCreateTime_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rgIpLanAddrLeaseCreateTime_undo( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrLeaseCreateTime_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rgIpLanAddrLeaseCreateTime undo.
     */
    /*
     * copy rgIpLanAddrLeaseCreateTime and rgIpLanAddrLeaseCreateTime_len data
     * set rowreq_ctx->data.rgIpLanAddrLeaseCreateTime from rowreq_ctx->undo->rgIpLanAddrLeaseCreateTime
     */
    memcpy( rowreq_ctx->data.rgIpLanAddrLeaseCreateTime, rowreq_ctx->undo->rgIpLanAddrLeaseCreateTime,
            (rowreq_ctx->undo->rgIpLanAddrLeaseCreateTime_len * sizeof(rowreq_ctx->data.rgIpLanAddrLeaseCreateTime[0])));
    rowreq_ctx->data.rgIpLanAddrLeaseCreateTime_len = rowreq_ctx->undo->rgIpLanAddrLeaseCreateTime_len;

    
    return MFD_SUCCESS;
} /* rgIpLanAddrLeaseCreateTime_undo */

/*---------------------------------------------------------------------
 * BRCM-RG-IP-MIB::rgIpLanAddrEntry.rgIpLanAddrLeaseExpireTime
 * rgIpLanAddrLeaseExpireTime is subid 5 of rgIpLanAddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4413.2.2.2.1.7.2.3.1.1.5
 * Description:
This is the date and time when the LAN IP address lease
        will expire.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 2d-1d-1d,1d:1d:1d.1d,1a1d:1d
 *
 * Ranges:  8, 11;
 *
 * Its syntax is DateAndTime (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 11)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rgIpLanAddrLeaseExpireTime_val_ptr
 *        A char containing the new value.
 * @param rgIpLanAddrLeaseExpireTime_val_ptr_len
 *        The size (in bytes) of the data pointed to by rgIpLanAddrLeaseExpireTime_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rgIpLanAddrTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rgIpLanAddrLeaseExpireTime).
 *    The length is in (one of) the range set(s):  8, 11
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rgIpLanAddrLeaseExpireTime_check_value( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx, char *rgIpLanAddrLeaseExpireTime_val_ptr,  size_t rgIpLanAddrLeaseExpireTime_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrLeaseExpireTime_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rgIpLanAddrLeaseExpireTime_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rgIpLanAddrLeaseExpireTime value.
     */

    return MFD_SUCCESS; /* rgIpLanAddrLeaseExpireTime value not illegal */
} /* rgIpLanAddrLeaseExpireTime_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rgIpLanAddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rgIpLanAddrTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rgIpLanAddrLeaseExpireTime_undo_setup( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrLeaseExpireTime_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rgIpLanAddrLeaseExpireTime undo.
     */
    /*
     * copy rgIpLanAddrLeaseExpireTime and rgIpLanAddrLeaseExpireTime_len data
     * set rowreq_ctx->undo->rgIpLanAddrLeaseExpireTime from rowreq_ctx->data.rgIpLanAddrLeaseExpireTime
     */
    memcpy( rowreq_ctx->undo->rgIpLanAddrLeaseExpireTime, rowreq_ctx->data.rgIpLanAddrLeaseExpireTime,
            (rowreq_ctx->data.rgIpLanAddrLeaseExpireTime_len * sizeof(rowreq_ctx->undo->rgIpLanAddrLeaseExpireTime[0])));
    rowreq_ctx->undo->rgIpLanAddrLeaseExpireTime_len = rowreq_ctx->data.rgIpLanAddrLeaseExpireTime_len;


    return MFD_SUCCESS;
} /* rgIpLanAddrLeaseExpireTime_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rgIpLanAddrLeaseExpireTime_val_ptr
 *        A char containing the new value.
 * @param rgIpLanAddrLeaseExpireTime_val_ptr_len
 *        The size (in bytes) of the data pointed to by rgIpLanAddrLeaseExpireTime_val_ptr
 */
int
rgIpLanAddrLeaseExpireTime_set( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx, char *rgIpLanAddrLeaseExpireTime_val_ptr,  size_t rgIpLanAddrLeaseExpireTime_val_ptr_len )
{

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrLeaseExpireTime_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rgIpLanAddrLeaseExpireTime_val_ptr);

    /*
     * TODO:461:M: |-> Set rgIpLanAddrLeaseExpireTime value.
     * set rgIpLanAddrLeaseExpireTime value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.rgIpLanAddrLeaseExpireTime, rgIpLanAddrLeaseExpireTime_val_ptr, rgIpLanAddrLeaseExpireTime_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.rgIpLanAddrLeaseExpireTime_len = rgIpLanAddrLeaseExpireTime_val_ptr_len / sizeof(rgIpLanAddrLeaseExpireTime_val_ptr[0]);

    return MFD_SUCCESS;
} /* rgIpLanAddrLeaseExpireTime_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rgIpLanAddrLeaseExpireTime_undo( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrLeaseExpireTime_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rgIpLanAddrLeaseExpireTime undo.
     */
    /*
     * copy rgIpLanAddrLeaseExpireTime and rgIpLanAddrLeaseExpireTime_len data
     * set rowreq_ctx->data.rgIpLanAddrLeaseExpireTime from rowreq_ctx->undo->rgIpLanAddrLeaseExpireTime
     */
    memcpy( rowreq_ctx->data.rgIpLanAddrLeaseExpireTime, rowreq_ctx->undo->rgIpLanAddrLeaseExpireTime,
            (rowreq_ctx->undo->rgIpLanAddrLeaseExpireTime_len * sizeof(rowreq_ctx->data.rgIpLanAddrLeaseExpireTime[0])));
    rowreq_ctx->data.rgIpLanAddrLeaseExpireTime_len = rowreq_ctx->undo->rgIpLanAddrLeaseExpireTime_len;

    
    return MFD_SUCCESS;
} /* rgIpLanAddrLeaseExpireTime_undo */

/*---------------------------------------------------------------------
 * BRCM-RG-IP-MIB::rgIpLanAddrEntry.rgIpLanAddrHostName
 * rgIpLanAddrHostName is subid 6 of rgIpLanAddrEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.4413.2.2.2.1.7.2.3.1.1.6
 * Description:
This is the Host Name of the LAN IP address, based on DCHP 
       option 12.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   1
 *   hint: 255t
 *
 * Ranges:  0 - 80;
 *
 * Its syntax is SnmpAdminString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 80)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param rgIpLanAddrHostName_val_ptr
 *        A char containing the new value.
 * @param rgIpLanAddrHostName_val_ptr_len
 *        The size (in bytes) of the data pointed to by rgIpLanAddrHostName_val_ptr
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * rgIpLanAddrTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_OCTET_STR
 *    The length is < sizeof(rowreq_ctx->data.rgIpLanAddrHostName).
 *    The length is in (one of) the range set(s):  0 - 80
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
rgIpLanAddrHostName_check_value( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx, char *rgIpLanAddrHostName_val_ptr,  size_t rgIpLanAddrHostName_val_ptr_len)
{
    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrHostName_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rgIpLanAddrHostName_val_ptr);

    /*
     * TODO:441:o: |-> Check for valid rgIpLanAddrHostName value.
     */

    return MFD_SUCCESS; /* rgIpLanAddrHostName value not illegal */
} /* rgIpLanAddrHostName_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (rgIpLanAddrTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * rgIpLanAddrTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
rgIpLanAddrHostName_undo_setup( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrHostName_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup rgIpLanAddrHostName undo.
     */
    /*
     * copy rgIpLanAddrHostName and rgIpLanAddrHostName_len data
     * set rowreq_ctx->undo->rgIpLanAddrHostName from rowreq_ctx->data.rgIpLanAddrHostName
     */
    memcpy( rowreq_ctx->undo->rgIpLanAddrHostName, rowreq_ctx->data.rgIpLanAddrHostName,
            (rowreq_ctx->data.rgIpLanAddrHostName_len * sizeof(rowreq_ctx->undo->rgIpLanAddrHostName[0])));
    rowreq_ctx->undo->rgIpLanAddrHostName_len = rowreq_ctx->data.rgIpLanAddrHostName_len;


    return MFD_SUCCESS;
} /* rgIpLanAddrHostName_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param rgIpLanAddrHostName_val_ptr
 *        A char containing the new value.
 * @param rgIpLanAddrHostName_val_ptr_len
 *        The size (in bytes) of the data pointed to by rgIpLanAddrHostName_val_ptr
 */
int
rgIpLanAddrHostName_set( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx, char *rgIpLanAddrHostName_val_ptr,  size_t rgIpLanAddrHostName_val_ptr_len )
{

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrHostName_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);
    netsnmp_assert(NULL != rgIpLanAddrHostName_val_ptr);

    /*
     * TODO:461:M: |-> Set rgIpLanAddrHostName value.
     * set rgIpLanAddrHostName value in rowreq_ctx->data
     */
    memcpy( rowreq_ctx->data.rgIpLanAddrHostName, rgIpLanAddrHostName_val_ptr, rgIpLanAddrHostName_val_ptr_len );
    /** convert bytes to number of char */
    rowreq_ctx->data.rgIpLanAddrHostName_len = rgIpLanAddrHostName_val_ptr_len / sizeof(rgIpLanAddrHostName_val_ptr[0]);

    return MFD_SUCCESS;
} /* rgIpLanAddrHostName_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
rgIpLanAddrHostName_undo( rgIpLanAddrTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:rgIpLanAddrTable:rgIpLanAddrHostName_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up rgIpLanAddrHostName undo.
     */
    /*
     * copy rgIpLanAddrHostName and rgIpLanAddrHostName_len data
     * set rowreq_ctx->data.rgIpLanAddrHostName from rowreq_ctx->undo->rgIpLanAddrHostName
     */
    memcpy( rowreq_ctx->data.rgIpLanAddrHostName, rowreq_ctx->undo->rgIpLanAddrHostName,
            (rowreq_ctx->undo->rgIpLanAddrHostName_len * sizeof(rowreq_ctx->data.rgIpLanAddrHostName[0])));
    rowreq_ctx->data.rgIpLanAddrHostName_len = rowreq_ctx->undo->rgIpLanAddrHostName_len;

    
    return MFD_SUCCESS;
} /* rgIpLanAddrHostName_undo */

/** @} */
